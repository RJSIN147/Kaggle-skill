# Competition: $slug

> Static competition facts — the "constitution". Captured once and rarely changed.
> **This is a STUB.** The evaluation metric, data schema, rules, submission limits,
> and CV scheme are filled in during Phase 2 (`kaggle-exp` competition setup +
> data download). Phase 1 records only the slug.

- **Slug:** `$slug`
- **URL:** https://www.kaggle.com/competitions/$slug

## Evaluation metric
_TODO (Phase 2): the exact metric and how leaderboard score is computed._

## Data schema
_TODO (Phase 2): files, columns, target, id column._

## Rules & limits
_TODO (Phase 2): submission limits (typically ~5/day), external-data rules,
code-competition constraints._

## Cross-validation scheme
_TODO (Phase 2): the CV splitter that mirrors the leaderboard split
(e.g. StratifiedKFold / GroupKFold / TimeSeriesSplit)._
