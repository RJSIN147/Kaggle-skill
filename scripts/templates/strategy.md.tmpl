# Strategy — $slug

> Generated each cycle from control/ledger.jsonl — manual edits are overwritten.
> This stub stands in until the first `regen_strategy.py` cycle; from then on the doc is
> REGENERATED and FULLY OVERWRITTEN each cycle (D-12): tooling renders the FACTS
> (current-best by metric direction + the tried-list digest) from the ledger and splices
> the AI's `--reasoning-file` (hypothesis queue + next action). Do not hand-edit.

## Current best
_None yet — no experiments have been run._

## Hypothesis queue
1. _TODO: first idea to try (e.g. a LightGBM baseline with a leakage-safe CV split)._

## Log of moves
- Experiments are recorded in `control/ledger.jsonl`, one line each, with a
  written verdict at `experiments/exp-NNN/VERDICT.md`.
- Experiment ids are zero-padded and sequential: `exp-001`, `exp-002`, …  (D-11).

## Notes
_Discipline: CV-first. Track the CV→LB gap; ration submissions against CV signal._
