---
phase: 02-competition-context-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/kaggle_gateway.py
  - scripts/templates/settings.json.tmpl
  - scripts/templates/gitignore.tmpl
  - references/egress-allowlist.md
  - references/kaggle-cli-behavior.md
  - tests/test_egress_allowlist.py
  - tests/test_gateway.py
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "Every Phase 2 Kaggle CLI call routes through one gateway function (run_kaggle) that never echoes captured output (D-16)"
    - "api.kaggle.com is present in the workspace egress allowlist template so Phase 2 CLI calls are not sandbox-blocked"
    - "The cheap preflight probe reports whether the user has entered a competition without ever 403-ing or busy-looping"
    - "An unclassified 403 fails closed — it names BOTH the rules and phone-verification gates and never guesses (D-12)"
    - "Classification authors a framework-written, secret-free message and quarantines the raw CLI output to a gitignored control/raw/last-error.txt, never the terminal (D-11)"
  artifacts:
    - path: "scripts/kaggle_gateway.py"
      provides: "D-16 gateway: run_kaggle, preflight_entered, classify_gate, UI_GATE/LIMIT_NEEDS_USER constants, dump_last_error"
      contains: "def run_kaggle"
    - path: "scripts/templates/settings.json.tmpl"
      provides: "egress allowlist with api.kaggle.com"
      contains: "api.kaggle.com"
    - path: "references/egress-allowlist.md"
      provides: "corrected api.kaggle.com host row + correction-history entry"
      contains: "api.kaggle.com"
    - path: "tests/test_gateway.py"
      provides: "unit coverage for preflight_entered fuzzy-slug match, classify_gate fail-closed, exit-code constants"
      contains: "def test_"
    - path: "tests/test_egress_allowlist.py"
      provides: "assert api.kaggle.com in allowlist, no wildcard broadening"
      contains: "api.kaggle.com"
  key_links:
    - from: "scripts/kaggle_gateway.py"
      to: "kaggle CLI subprocess"
      via: "subprocess.run(['kaggle', *argv], capture_output=True, timeout=)"
      pattern: "subprocess\\.run"
    - from: "scripts/kaggle_gateway.py:preflight_entered"
      to: "competitions list --search <slug>"
      via: "exact-slug userHasEntered"
      pattern: "userHasEntered"
---

<objective>
Land the phase's blocking egress prerequisite and build the single Kaggle Gateway (D-16)
that every Phase 2 entry point routes its CLI calls through. Nothing else in Phase 2 can run
in a sandboxed workspace until `api.kaggle.com` is allowlisted, and no entry point may fork the
subprocess/no-echo/exit-code contract — they all call this gateway.

Purpose: `api.kaggle.com` is where CLI 2.2.3 routes `pages`/`files`/`list`/`download`
(VERIFIED-LIVE); it is missing from the Phase 1 allowlist, so a properly sandboxed workspace
blocks ALL of Phase 2. The gateway generalises the already-tested `check_credentials.run_kaggle_list()`
so gate detection, no-echo, and timeout bounding live in exactly one place.

Output: `scripts/kaggle_gateway.py`; the corrected `settings.json.tmpl` + `egress-allowlist.md`;
the extended `kaggle-cli-behavior.md`; the `gitignore.tmpl` ignore line for transient error dumps;
and two RED-first unit test files that go GREEN in this plan.
</objective>

## Phase Goal

**As a** Kaggle practitioner setting up a competition workspace, **I want to** capture a correct
machine-derived competition constitution and download the data locally — with UI-only gates cleared
and every ingested Kaggle text treated as untrusted — **so that** every downstream experiment reasons
from trustworthy competition facts without a prompt-injection or a wasted submission slot.

> This is the ROADMAP Phase 2 goal translated into a user story for a CLI-skill context (per the
> MVP-on-CLI-skill framing in project memory). This plan delivers the enabling capability the three
> operator-facing slices (capture / download / analyze) all depend on: Kaggle is reachable, and one
> gateway owns every call.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-competition-context-data/02-CONTEXT.md
@.planning/phases/02-competition-context-data/02-RESEARCH.md
@.planning/phases/02-competition-context-data/02-PATTERNS.md

<interfaces>
<!-- Exact signatures the executor generalises/reuses. Do NOT fork these — import or generalise. -->

From scripts/check_credentials.py (GENERALISE run_kaggle_list; COPY branch_remediation shape):
```python
def run_kaggle_list() -> tuple[int, str]:
    # subprocess.run(["kaggle","competitions","list"], capture_output=True, text=True, timeout=60)
    # except subprocess.TimeoutExpired: return 124, "kaggle competitions list timed out"
    # return proc.returncode, (proc.stdout or "") + "\n" + (proc.stderr or "")   # NEVER printed

def branch_remediation(combined: str, source: str) -> None:
    # matches `combined` (never echoes it); honest else: fall-through withholds raw output.
```

Target gateway surface (RESEARCH §Pattern 1 / §Pattern 2 / §Existing Code Integration):
```python
UI_GATE = 77          # sysexits EX_NOPERM — 403 rules/phone gate
LIMIT_NEEDS_USER = 78 # sysexits EX_CONFIG — submission-limit extraction failed (used by 02-02)
def run_kaggle(*argv: str, timeout: int = 60) -> tuple[int, str]: ...   # 127 if kaggle absent
def preflight_entered(slug: str) -> bool | None: ...  # True|False|None; exact-slug userHasEntered
def classify_gate(combined: str, slug: str) -> str: ...  # returns a framework-authored, secret-free message key
def dump_last_error(ws: Path, combined: str) -> Path: ...  # writes control/raw/last-error.txt (gitignored)
```

From scripts/init_workspace.py (allowlist auto-retrofit is union-merge; gitignore is create-if-absent):
```python
def merge_settings(current: dict, template: dict) -> dict:   # unions allowedDomains (auto-retrofits api.kaggle.com)
def _union_list(base: list, additions) -> list:
# .gitignore is written via create_if_absent(TEXT_TEMPLATES) — editing gitignore.tmpl does NOT retrofit
# an existing workspace; a line-level append-if-absent helper is needed for the last-error.txt ignore.
```

From tests/test_settings.py (analog for test_egress_allowlist.py):
```python
REQUIRED_HOSTS = {"www.kaggle.com","storage.googleapis.com","pypi.org","files.pythonhosted.org","github.com"}
def _domains(settings_path): return settings, set(settings["sandbox"]["network"]["allowedDomains"])
```

VERIFIED-LIVE 403 signature (CLI 2.2.3): download of an un-entered comp → exit 1, stdout empty,
stderr `403 Client Error: Forbidden for url: https://api.kaggle.com/v1/competitions.CompetitionApiService/DownloadDataFiles`.
The message is GENERIC — it cannot distinguish rules vs phone vs permission. Rules URL =
`https://www.kaggle.com/competitions/<slug>/rules`; phone URL = `https://www.kaggle.com/settings/phone`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests — egress allowlist membership + gateway pure-function contract</name>
  <files>tests/test_egress_allowlist.py, tests/test_gateway.py</files>
  <read_first>
    - tests/test_settings.py (REQUIRED_HOSTS / _domains shape to mirror)
    - tests/conftest.py (run_script, tmp_workspace fixtures; hermetic KAGGLE_* stripping)
    - scripts/templates/settings.json.tmpl (current allowedDomains — api.kaggle.com absent)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §Pattern 1/2, §5 403 table, §17 exit codes
  </read_first>
  <action>
    Write two RED unit files (they fail now — kaggle_gateway.py does not exist and settings.json.tmpl
    lacks api.kaggle.com).
    `tests/test_egress_allowlist.py`: run `init_workspace.py --slug titanic` via the `run_script`
    fixture, load the generated `.claude/settings.json` with the `_domains` helper shape, assert
    `"api.kaggle.com" in allowedDomains`, assert the existing REQUIRED_HOSTS still hold, and assert NO
    entry equals `"*"` or `"*.kaggle.com"` (narrow allowlist — no wildcard broadening).
    `tests/test_gateway.py`: import `kaggle_gateway`; monkeypatch its `run_kaggle` to return a canned
    `competitions list --search` JSON array. Assert `preflight_entered("titanic")` returns True when the
    exact-slug row has `userHasEntered=true`, False when false, and None when the slug is absent from a
    fuzzy result (e.g. a search that returns only `spaceship-titanic`). Assert `UI_GATE == 77` and
    `LIMIT_NEEDS_USER == 78`. Assert `classify_gate(combined, slug)` on the generic 403 signature returns
    a message that contains BOTH `/competitions/titanic/rules` AND `/settings/phone` and states the gate
    could not be classified (D-12 fail-closed), and NEVER contains the raw combined buffer.
    Do NOT place fenced code from RESEARCH into the assertions verbatim beyond the signatures given.
  </action>
  <verify>
    <automated>uv run pytest tests/test_egress_allowlist.py tests/test_gateway.py -q 2>&1 | grep -Eq "error|failed|no tests ran|ModuleNotFoundError" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_egress_allowlist.py` and `tests/test_gateway.py` both exist and are collected by pytest
    - Running them now fails/errors (RED) because `kaggle_gateway.py` is absent and the template lacks api.kaggle.com
    - `test_gateway.py` asserts `UI_GATE == 77` and `LIMIT_NEEDS_USER == 78`
    - `test_gateway.py` asserts `classify_gate` output contains both `/competitions/titanic/rules` and `/settings/phone`
    - `test_egress_allowlist.py` asserts `"api.kaggle.com"` membership AND asserts no `"*"` wildcard entry
  </acceptance_criteria>
  <done>Two RED test files committed; each is collectable and fails for the right reason (missing module / missing host).</done>
</task>

<task type="auto">
  <name>Task 2: BLOCKING egress fix — add api.kaggle.com; correct the stale allowlist claim</name>
  <files>scripts/templates/settings.json.tmpl, references/egress-allowlist.md</files>
  <read_first>
    - scripts/templates/settings.json.tmpl (the allowedDomains array to extend)
    - references/egress-allowlist.md (line 47 stale claim; the Correction history table format at the file's end)
    - scripts/init_workspace.py:156-246 (merge_settings/_union_list — confirms union-merge auto-retrofit)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §CRITICAL EGRESS FINDING
  </read_first>
  <action>
    Add `"api.kaggle.com"` to the `sandbox.network.allowedDomains` array in `settings.json.tmpl`
    (keep the array otherwise unchanged — narrow, no wildcard). In `references/egress-allowlist.md`:
    (a) rewrite the `www.kaggle.com` row at line 47 so it no longer asserts `**Not** api.kaggle.com` —
    the CLI 2.x endpoint host is `api.kaggle.com/v1/{service}/{request}` (VERIFIED-LIVE against CLI 2.2.3
    + kagglesdk `kaggle_env.py` PROD→api.kaggle.com); (b) add a new host row for `api.kaggle.com` in the
    "Allowed hosts and why each is on the list" table explaining it is the CLI 2.2.3 API endpoint host for
    pages/files/list/download; (c) add a row to the existing Correction history table
    (format `| Date | Claim that was wrong | What overturned it |`) dated 2026-07-10 recording that the
    "Not api.kaggle.com (stale for the 2.x CLI)" claim was VERIFIED WRONG for CLI 2.2.3 by the proxy-failure
    host probe. Do NOT delete the prior claim silently — this file documents corrections. Note that because
    `merge_settings` unions allowedDomains, re-running `init` auto-retrofits api.kaggle.com onto an existing
    workspace (unlike `.gitignore`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_egress_allowlist.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/templates/settings.json.tmpl` contains `"api.kaggle.com"` and contains no `"*"` bare-wildcard entry
    - `grep -c 'api.kaggle.com' references/egress-allowlist.md` is at least 2 (host row + correction row)
    - `references/egress-allowlist.md` no longer contains the substring `**Not** \`api.kaggle.com\``
    - The Correction history table has a new 2026-07-10 row citing CLI 2.2.3 / kagglesdk PROD host
    - `uv run pytest tests/test_egress_allowlist.py -q` exits 0 (GREEN)
  </acceptance_criteria>
  <done>api.kaggle.com is allowlisted for new + retrofitted workspaces; the stale reference claim is corrected with a recorded correction; the egress test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Kaggle Gateway (D-16) + transient-error gitignore hygiene + CLI-behavior reference</name>
  <files>scripts/kaggle_gateway.py, scripts/templates/gitignore.tmpl, references/kaggle-cli-behavior.md</files>
  <read_first>
    - scripts/check_credentials.py:342-422 (run_kaggle_list to generalise; branch_remediation honest else: to copy)
    - scripts/init_workspace.py:105-153 (create_if_absent — analog for the line-level gitignore helper)
    - scripts/leak_scan.py (confirms last-error.txt must stay gitignored yet leak-guard-covered if ever staged)
    - references/kaggle-cli-behavior.md (honest-provenance + observed-signatures convention to extend)
    - scripts/templates/gitignore.tmpl (current ignore lines; data/ + secrets)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §Pattern 1/2, §Existing Code Integration
  </read_first>
  <behavior>
    - run_kaggle("competitions","list") returns (rc, combined) and returns 127 when `shutil.which("kaggle")` is None; TimeoutExpired maps to (124, "kaggle timed out"); combined is stdout+"\n"+stderr and is never printed by the gateway
    - preflight_entered(slug): parses a mocked `competitions list --search` JSON array, matches the EXACT slug via ref.rsplit('/',1)[-1] (never row[0] — search is fuzzy), returns bool(userHasEntered); returns None on non-zero rc or unparseable/absent slug
    - classify_gate(combined, slug): positively classifies ONLY the rules gate (via preflight); an unclassified 403 returns a secret-free message naming BOTH /competitions/<slug>/rules and /settings/phone and stating it may be a genuine permission error; never contains the raw combined buffer
    - dump_last_error(ws, combined): writes ws/control/raw/last-error.txt and ensures control/raw/last-error.txt is present in ws/.gitignore via an append-line-if-absent helper (idempotent)
    - UI_GATE == 77 and LIMIT_NEEDS_USER == 78 are module-level constants
  </behavior>
  <action>
    Create `scripts/kaggle_gateway.py` (stdlib-only, self-locating `SCRIPT_DIR = Path(__file__).resolve().parent`,
    importable — no argparse main required since it is a library, but keep it import-safe). Generalise
    `run_kaggle_list()` into `run_kaggle(*argv, timeout=60)` preserving the `shutil.which` guard (return
    `127, "kaggle CLI not found on PATH"`), the `TimeoutExpired → (124, "kaggle timed out")` map, the
    `(returncode, stdout+"\n"+stderr)` return, and the never-echo rule. Add `preflight_entered(slug)` per
    RESEARCH §Pattern 2 (fuzzy search → exact-slug `userHasEntered`, `True|False|None`). Add
    `classify_gate(combined, slug)` copying `branch_remediation`'s match-don't-echo shape, whose honest
    `else:` fall-through is the literal model for D-12: name BOTH `https://www.kaggle.com/competitions/{slug}/rules`
    and `https://www.kaggle.com/settings/phone`, state the gate could not be classified and may be a genuine
    permission error, and never echo `combined`. This pair (classify_gate + dump_last_error) IS D-11's
    posture — classify → author our own framework-written, secret-free message → quarantine the raw CLI output
    to `control/raw/` (never the terminal). Define module constants `UI_GATE = 77`, `LIMIT_NEEDS_USER = 78`
    (sysexits EX_NOPERM / EX_CONFIG; 124 is already the timeout code — do not reuse). Add `dump_last_error(ws, combined)`
    that writes `ws/control/raw/last-error.txt` and ensures that ignore line exists in `ws/.gitignore` via a
    line-level append-if-absent helper (analog of `create_if_absent`, line-granular — because `.gitignore` is
    create-if-absent and editing the template does not retrofit an existing workspace).
    Add `control/raw/last-error.txt` to `scripts/templates/gitignore.tmpl` (for NEW workspaces) while leaving
    provenance JSON (`control/raw/*.json`) TRACKED (D-11: transient error dumps ignored, provenance tracked).
    Extend `references/kaggle-cli-behavior.md` (do NOT create a
    parallel file): add rows for the observed 403 gate signature (generic `403 ...DownloadDataFiles` on stderr,
    exit 1; rules gate detected via `userHasEntered`; phone gate UNVERIFIED → fail-closed) and the two
    live-verified CLI facts (`competitions pages --content` exists; `competitions download` has NO `--unzip`),
    each carrying the same sanitized-capture provenance note the existing rows use.
  </action>
  <verify>
    <automated>uv run pytest tests/test_gateway.py -q && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/kaggle_gateway.py` contains `def run_kaggle(`, `def preflight_entered(`, `def classify_gate(`, `def dump_last_error(`
    - `grep -Eq 'UI_GATE\s*=\s*77' scripts/kaggle_gateway.py` and `grep -Eq 'LIMIT_NEEDS_USER\s*=\s*78' scripts/kaggle_gateway.py`
    - `scripts/templates/gitignore.tmpl` contains `control/raw/last-error.txt`
    - `scripts/templates/gitignore.tmpl` does NOT ignore `control/raw/*.json` (provenance stays tracked)
    - `grep -c 'unzip' references/kaggle-cli-behavior.md` >= 1 and the 403 signature row is present
    - `uv run pytest tests/test_gateway.py -q` exits 0; full `uv run pytest tests/ -x -q` stays green
  </acceptance_criteria>
  <done>The gateway owns every CLI call surface (run/preflight/classify/error-dump) with no-echo + fail-closed; new workspaces ignore last-error.txt and a helper retrofits existing ones; the CLI-behavior reference records the 403 + two CLI facts; both gateway tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill script → kaggle CLI subprocess | Untrusted network responses + possibly token-shaped strings cross back on stdout/stderr |
| workspace → api.kaggle.com egress | The sandbox allowlist decides whether Phase 2 traffic leaves at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-EGRESS | Information Disclosure | settings.json.tmpl allowlist | mitigate | Add ONLY `api.kaggle.com` (VERIFIED host); no wildcard broadening; egress-allowlist.md §"Auto-accept defeats the allowlist" remains the operational caveat |
| T-02-GUESS | Repudiation | kaggle_gateway.classify_gate | mitigate | Generic 403 cannot name a gate; fail closed — name BOTH rules + phone URLs, state may be a permission error, never pattern-match "phone" into the 403 string (D-12) |
| T-02-LEAK | Information Disclosure | kaggle_gateway.run_kaggle / dump_last_error | mitigate | Combined stdout+stderr never echoed; raw dumped to gitignored `control/raw/last-error.txt` (D-11); leak guard still covers it if ever staged |
| T-02-DOS | Denial of Service | preflight_entered | mitigate | Cheap single `list --search` probe; never busy-loops an authenticated endpoint (loop absence enforced in 02-03) |
| T-02-SC | Tampering | package installs | accept | No new skill packages installed this plan (stdlib-only); RESEARCH Package Legitimacy Audit shows none [SUS] |
</threat_model>

<verification>
- `uv run pytest tests/test_egress_allowlist.py tests/test_gateway.py -q` → GREEN
- `uv run pytest tests/ -x -q` → full suite stays green (no Phase 1 regression)
- `grep 'api.kaggle.com' scripts/templates/settings.json.tmpl references/egress-allowlist.md` → present in both
- `grep -E 'def (run_kaggle|preflight_entered|classify_gate|dump_last_error)' scripts/kaggle_gateway.py` → all four present
</verification>

<success_criteria>
- api.kaggle.com is on the allowlist template (and auto-retrofits via union-merge); the stale reference claim is corrected with a recorded correction-history entry.
- One gateway owns run_kaggle / preflight_entered / classify_gate / dump_last_error with the Phase 1 no-echo + timeout + exit-code contract (D-16).
- Gate classification authors a secret-free framework message and quarantines the raw output to control/raw/ (D-11); UI_GATE=77 and LIMIT_NEEDS_USER=78 are defined for downstream plans to import.
- Transient error dumps are gitignored (template + line-level retrofit helper); provenance JSON stays tracked.
- Both new test files are GREEN; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/02-competition-context-data/02-01-SUMMARY.md` when done.
</output>
