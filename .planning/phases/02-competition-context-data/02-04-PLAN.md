---
phase: 02-competition-context-data
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - scripts/cv_evidence.py
  - scripts/analyze_data.py
  - scripts/templates/pyproject.toml.tmpl
  - tests/test_cv_evidence.py
  - tests/fixtures/cv/grouped_train.csv
  - tests/fixtures/cv/grouped_test.csv
  - tests/fixtures/cv/temporal_train.csv
  - tests/fixtures/cv/temporal_test.csv
  - tests/fixtures/cv/imbalanced_train.csv
  - tests/fixtures/cv/imbalanced_test.csv
autonomous: true
requirements: [COMP-01]
user_setup:
  - service: workspace-ml-env
    why: "Real adversarial validation needs pandas + scikit-learn in the workspace ML env; the skill never runtime-installs"
    dashboard_config:
      - task: "Run `uv sync` in the workspace to install the declared ML floors (pandas>=2.2, scikit-learn>=1.5) for real adversarial validation; if skipped, analyze exits 0 and records AV as SKIPPED"
        location: "workspace pyproject.toml"
must_haves:
  truths:
    - "Tooling emits structural CV evidence; the AI reasons over it; a tooling call writes config.json cv.scheme as an enum — the AI never hand-writes the field"
    - "The target column is identified mechanically as columns(train) − columns(test) − id_column, and the derivation is recorded"
    - "The recommended CV scheme follows group > temporal > stratified > plain from the data structure"
    - "Real adversarial validation runs under the workspace ML env; if that env is absent, analyze still exits 0 and records AV as SKIPPED (never pip-installs at runtime)"
    - "cv-evidence.json is written to control/raw/ as a tracked provenance artifact"
  artifacts:
    - path: "scripts/cv_evidence.py"
      provides: "stdlib structural evidence + recommend_cv decision order + target derivation"
      contains: "def recommend_cv"
    - path: "scripts/analyze_data.py"
      provides: "schema + CV scheme (tooling-written enum) + adversarial validation (uv run) / stdlib fallback"
      contains: "def main"
    - path: "scripts/templates/pyproject.toml.tmpl"
      provides: "declared ML floors pandas>=2.2, scikit-learn>=1.5"
      contains: "scikit-learn"
    - path: "tests/test_cv_evidence.py"
      provides: "evidence → CV scheme decision table + target derivation + ML-absent SKIPPED path"
      contains: "def test_"
  key_links:
    - from: "scripts/analyze_data.py"
      to: "control/config.json"
      via: "write_control_json enum-validated cv.scheme"
      pattern: "write_control_json"
    - from: "scripts/analyze_data.py"
      to: "workspace ML env"
      via: "uv run (real adversarial validation) / stdlib fallback"
      pattern: "uv"
    - from: "scripts/cv_evidence.py"
      to: "control/raw/cv-evidence.json"
      via: "tracked provenance write"
      pattern: "cv-evidence"
---

<objective>
Deliver the analysis slice: from the downloaded CSVs, emit structural CV evidence, let the AI commit
to a scheme (tooling writes the enum), and produce an adversarial-validation finding — completing
`competition.md` so success criterion 1 holds end to end. This is the last piece of the machine-derived
constitution.

Purpose: CV scheme is a structural fact, so it follows the same "tooling writes, AI never hand-writes"
posture Phase 3 applies to numeric results, applied one phase early (D-05). Real adversarial validation
(LogisticRegression on train=0/test=1, roc_auc_score) catches JOINT distribution shift — the failure
mode that destroys CV→LB correlation (the exact quantity SCORE-02 tracks). Per-column marginal shift
is a weaker artifact; if only that runs, competition.md must say SKIPPED, not imply AV ran (D-06).

Output: `scripts/cv_evidence.py`; `scripts/analyze_data.py`; the ML floors in `pyproject.toml.tmpl`;
the CV-evidence test + tiny grouped/temporal/imbalanced fixture CSVs.
</objective>

## Phase Goal

**As a** Kaggle practitioner setting up a competition workspace, **I want to** capture a correct
machine-derived competition constitution and download the data locally — with UI-only gates cleared
and every ingested Kaggle text treated as untrusted — **so that** every downstream experiment reasons
from trustworthy competition facts without a prompt-injection or a wasted submission slot.

> Slice delivered here: after this plan an operator can run `analyze_data.py` on the downloaded data and
> get a tooling-written CV scheme plus an adversarial-validation finding in competition.md — the
> constitution is complete.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-competition-context-data/02-CONTEXT.md
@.planning/phases/02-competition-context-data/02-RESEARCH.md
@.planning/phases/02-competition-context-data/02-PATTERNS.md
@.planning/phases/02-competition-context-data/02-02-SUMMARY.md
@.planning/phases/02-competition-context-data/02-03-SUMMARY.md

<interfaces>
<!-- Contracts this slice consumes/produces. competition.md structure comes from 02-02. -->

From scripts/init_workspace.py (REUSE for the tracked JSON + enum config write):
```python
def write_control_json(path, desired) -> str: ...   # enum-validated cv.scheme; deep-merge; never clobber edits
class MalformedControlJSON(Exception): ...
def create_if_absent(path, content) -> str: ...
```

CV recommendation decision order (RESEARCH §Code Examples / §Decision Tables — sklearn semantics):
```python
def recommend_cv(evidence: dict) -> str:
    # group > temporal > stratified > plain
    # group_candidates      → "GroupKFold"
    # datetime + train<test → "TimeSeriesSplit"
    # target + classification (esp imbalanced) → "StratifiedKFold"
    # else                  → "KFold"
```
Target column (D-07): `columns(train) − columns(test) − id_column`; record the derivation in cv-evidence.json.

Adversarial validation (RESEARCH §Code Examples, ML env only): concat train(0)/test(1),
ColumnTransformer(num: median-impute+scale; cat: mode-impute+OneHot max_categories=20),
LogisticRegression(max_iter=1000), cross_val_predict cv=5 → roc_auc_score. Interpret ~0.5 = no shift,
>~0.8 = strong shift; row cap 50_000 (RandomState(0)). State the threshold in competition.md next to the number.

Current pyproject.toml.tmpl has `dependencies = []`. Floors to declare (CLAUDE.md §Version Compatibility —
FLOORS not newest majors; pandas 3.0 is a breaking major, numpy 2.5.1 needs Py≥3.12 while floor is 3.11):
`pandas>=2.2`, `scikit-learn>=1.5` (numpy>=1.26 comes transitively).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests + tiny fixtures — evidence→CV decision table, target derivation, ML-absent SKIPPED</name>
  <files>tests/test_cv_evidence.py, tests/fixtures/cv/grouped_train.csv, tests/fixtures/cv/grouped_test.csv, tests/fixtures/cv/temporal_train.csv, tests/fixtures/cv/temporal_test.csv, tests/fixtures/cv/imbalanced_train.csv, tests/fixtures/cv/imbalanced_test.csv</files>
  <read_first>
    - tests/conftest.py (run_script, seeded_workspace, tmp_workspace fixtures)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §Code Examples (recommend_cv, adversarial_auc), §Decision Tables (CV scheme)
    - .planning/phases/02-competition-context-data/02-VALIDATION.md (C1 rows)
    - scripts/init_workspace.py:132-153 (write_control_json enum write to assert against)
  </read_first>
  <action>
    Create six tiny CSV fixtures under `tests/fixtures/cv/` (a handful of rows each, stdlib-writable):
    a GROUPED pair (a repeated entity/`group_id` column shared across rows, target present in train only),
    a TEMPORAL pair (a parseable `date` column with test dates strictly after train), and an IMBALANCED
    pair (a binary classification `target` with a skewed class ratio). Each train has exactly one column
    the test lacks besides the id (the target), so target derivation is unambiguous.
    Write `tests/test_cv_evidence.py` (RED): run `cv_evidence.py --workspace <ws>` (data mocked to point at
    each fixture pair) and assert `control/raw/cv-evidence.json` records: the derived target column
    (= columns(train) − columns(test) − id), the group candidates / datetime columns / class balance, and a
    `recommend` value that is `GroupKFold` for the grouped pair, `TimeSeriesSplit` for the temporal pair, and
    `StratifiedKFold` for the imbalanced pair. Assert `recommend_cv` follows the group>temporal>stratified>plain
    order (import it directly). Add a test that when the workspace ML env is absent, `analyze_data.py` still
    exits 0 and `competition.md` (or the recorded status) contains `adversarial validation: SKIPPED`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_evidence.py -q 2>&1 | grep -Eq "failed|error|no tests ran|ModuleNotFoundError" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Six fixture CSVs exist under `tests/fixtures/cv/` (grouped / temporal / imbalanced train+test pairs), each valid CSV
    - Each train CSV has exactly one non-id column absent from its test CSV (the target)
    - `tests/test_cv_evidence.py` asserts the decision table: grouped→GroupKFold, temporal→TimeSeriesSplit, imbalanced→StratifiedKFold
    - `tests/test_cv_evidence.py` asserts the target-column derivation is recorded in cv-evidence.json
    - `tests/test_cv_evidence.py` asserts the ML-absent path yields exit 0 + an `adversarial validation: SKIPPED` record
    - The file fails now (RED) because `cv_evidence.py` / `analyze_data.py` do not exist
  </acceptance_criteria>
  <done>RED CV-evidence tests + tiny grouped/temporal/imbalanced fixtures are committed and fail for the right reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cv_evidence.py — stdlib structural evidence + mechanical CV recommendation</name>
  <files>scripts/cv_evidence.py</files>
  <read_first>
    - scripts/init_workspace.py:105-153 (write_control_json / create_if_absent for the tracked cv-evidence.json write)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §Code Examples (recommend_cv decision order), §Decision Tables, D-07 target derivation
    - tests/test_cv_evidence.py (the decision-table + derivation contract from Task 1)
  </read_first>
  <behavior>
    - reads train/test CSVs (stdlib csv module — this is stdlib-only plumbing, NOT the ML step); derives the target as columns(train) − columns(test) − id_column and records the derivation
    - detects group candidates (a column whose values repeat across many rows / shared entity), datetime-parseable columns, target class balance, and train/test id overlap → writes them to control/raw/cv-evidence.json (TRACKED)
    - recommend_cv(evidence) returns GroupKFold > TimeSeriesSplit > StratifiedKFold > KFold in that priority order
    - cv-evidence.json includes the mechanical `recommend` value; the AI reasons over it and a later tooling call (analyze) writes cv.scheme — cv_evidence itself never writes config.json.cv.scheme
  </behavior>
  <action>
    Create `scripts/cv_evidence.py` (stdlib-only, self-locating, `--workspace` argparse, importable
    `recommend_cv`). Load train/test with the stdlib `csv` module (plumbing stays stdlib — D-06). Derive the
    target column mechanically (`columns(train) − columns(test) − id_column`, D-07) and record the derivation.
    Compute structural evidence: repeat-id/group-column candidates, datetime-parseable columns, target class
    balance (classification vs regression), and train/test id overlap. Implement `recommend_cv(evidence)` with
    the decision order group > temporal > stratified > plain (RESEARCH §Code Examples). Write
    `control/raw/cv-evidence.json` (TRACKED provenance, D-03) via the reused JSON-write path — never
    `git add -A`. Do NOT write `config.json.cv.scheme` here (that is analyze's tooling call after the AI reasons).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_evidence.py -q -k "evidence or recommend or target"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/cv_evidence.py` contains `def recommend_cv(` and writes `control/raw/cv-evidence.json`
    - `recommend_cv` returns GroupKFold/TimeSeriesSplit/StratifiedKFold/KFold following the documented priority order
    - cv-evidence.json records the mechanically derived target column and the group/datetime/class-balance/id-overlap evidence
    - `cv_evidence.py` does NOT write `config.json` `cv.scheme` (that is analyze's job)
    - The evidence/recommendation/target-derivation subset of `tests/test_cv_evidence.py` is GREEN
  </acceptance_criteria>
  <done>cv_evidence emits a tracked evidence artifact with a mechanical CV recommendation and an auditable target derivation; the evidence tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: analyze_data.py — schema + tooling-written cv.scheme + AV (uv run) with graceful degrade</name>
  <files>scripts/analyze_data.py, scripts/templates/pyproject.toml.tmpl</files>
  <read_first>
    - scripts/check_credentials.py:455-458 (CLI-absent graceful-degrade branch to model the ML-absent path on)
    - scripts/init_workspace.py:132-153 (write_control_json — enum-validated cv.scheme write)
    - scripts/cv_evidence.py (evidence + recommend_cv — from Task 2)
    - scripts/templates/pyproject.toml.tmpl (dependencies = [] to extend with floors)
    - scripts/templates/competition.md.tmpl (the CV scheme + Adversarial validation sections to fill — from 02-02)
    - .planning/phases/02-competition-context-data/02-RESEARCH.md §Code Examples (adversarial_auc, ML env only), §Standard Stack (floors), §Environment Availability (fallback)
  </read_first>
  <behavior>
    - analyze reads control/raw/cv-evidence.json, and a tooling call writes config.json.cv.scheme via write_control_json as an enum ∈ {GroupKFold,TimeSeriesSplit,StratifiedKFold,KFold} — the AI never hand-writes the field
    - it captures the data schema (files, columns, target, id column; discretion on dtypes/null/cardinality depth) into competition.md's Data schema section
    - it attempts real adversarial validation via `uv run` in the workspace ML env (LogisticRegression on train=0/test=1, roc_auc_score, row cap 50_000); records the AUC and the ~0.8 strong-shift threshold in competition.md
    - if pandas/scikit-learn are absent (uv run non-zero / import error), it still exits 0, emits the stdlib marginal-shift report, and records `adversarial validation: SKIPPED (ML env absent; run uv sync)` — never runtime pip install
    - all writes into competition.md are safe-merge (curated edits survive)
  </behavior>
  <action>
    Create `scripts/analyze_data.py` (stdlib-only PLUMBING that shells to `uv run` for the ML step — the ONLY
    `uv run` caller in the skill; self-locating, `--workspace` argparse, `raise SystemExit(main())`). Read
    `control/raw/cv-evidence.json`; a tooling call writes `config.json` `cv.scheme` (enum-validated via
    `write_control_json`) — the AI reasons over the evidence but never hand-writes the field (D-05). Capture the
    data schema into competition.md's Data schema section. Attempt REAL adversarial validation by shelling to
    `uv run` (the adversarial_auc code from RESEARCH §Code Examples: LogisticRegression train=0/test=1,
    roc_auc_score, cap 50_000); write the AUC + the stated ~0.8 strong-shift threshold into the Adversarial
    validation section. If the workspace ML env is absent (uv run fails / import error), DO NOT crash and DO NOT
    pip-install: exit 0, emit the stdlib marginal-shift report, and record
    `adversarial validation: SKIPPED (ML env absent; run uv sync)` (D-06 / Phase 1 D-07 flag-don't-abort). All
    competition.md writes are safe-merge. Extend `scripts/templates/pyproject.toml.tmpl` `[project.dependencies]`
    to declare `pandas>=2.2` and `scikit-learn>=1.5` (FLOORS, not newest majors — Kaggle-image parity; do NOT
    pin pandas 3.0 or numpy 2.5.1). Update the template comment so it no longer says the ML stack is deferred
    to Phase 3 for these two floors.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cv_evidence.py -q && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/analyze_data.py` contains `def main(` and writes `cv.scheme` via `write_control_json` (grep: `write_control_json`)
    - `scripts/analyze_data.py` shells to `uv run` for AV and degrades to exit 0 + `adversarial validation: SKIPPED` when the ML env is absent (grep: `SKIPPED`)
    - `scripts/analyze_data.py` contains NO runtime `pip install` (grep for `pip install` → none)
    - `scripts/templates/pyproject.toml.tmpl` contains `pandas>=2.2` and `scikit-learn>=1.5`
    - `uv run pytest tests/test_cv_evidence.py -q` exits 0; full `uv run pytest tests/ -x -q` stays green
  </acceptance_criteria>
  <done>analyze_data completes the constitution — tooling-written cv.scheme + a real (or honestly-SKIPPED) adversarial-validation finding — with graceful degrade and no runtime installs; the CV tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data CSVs → analysis | Downloaded data is untrusted input; schema/CV/AV derive from it |
| structural fact → config.json | cv.scheme is a machine fact Phase 3 will trust |
| workspace ML env → skill | The skill shells to `uv run`; the env may be absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-INTEGRITY-02 | Integrity | config.json cv.scheme | mitigate | Tooling writes the enum (write_control_json); AI reasons over cv-evidence.json but never hand-writes the field (D-05) |
| T-02-AVAIL-01 | Denial of Service | AV under uv run | mitigate | Flag-don't-abort: ML env absent → exit 0 + `AV: SKIPPED` marginal-shift report; never crash |
| T-02-SC-01 | Tampering | workspace ML deps | accept | pandas/scikit-learn are canonical, high-download, official-org packages (RESEARCH Package Legitimacy Audit: none [SUS]); declared as FLOORS; user runs `uv sync` under consent; the skill NEVER runtime pip-installs |
| T-02-HONESTY-01 | Repudiation | AV vs marginal-shift labeling | mitigate | If only the stdlib marginal report ran, competition.md says SKIPPED — never implies real AV ran (D-06) |
</threat_model>

<verification>
- `uv run pytest tests/test_cv_evidence.py -q` → GREEN
- `uv run pytest tests/ -x -q` → full suite green
- `grep -E 'write_control_json|SKIPPED' scripts/analyze_data.py` → both present
- `grep -Eq 'pip install' scripts/analyze_data.py && echo BAD || echo NONE` → NONE
- `grep -E 'pandas>=2.2|scikit-learn>=1.5' scripts/templates/pyproject.toml.tmpl` → both present
</verification>

<success_criteria>
- CV scheme is derived from the data structure (group/temporal/stratified/plain), written by tooling as an enum; the AI never hand-writes it.
- The target column is derived mechanically and the derivation is recorded in cv-evidence.json.
- Real adversarial validation runs under the workspace ML env; when that env is absent, analyze exits 0 and records AV as SKIPPED — never runtime-installs.
- competition.md now holds the metric, schema, rules, provenance-tagged limit, CV scheme, and an AV finding (criterion 1 complete).
</success_criteria>

<output>
Create `.planning/phases/02-competition-context-data/02-04-SUMMARY.md` when done.
</output>
