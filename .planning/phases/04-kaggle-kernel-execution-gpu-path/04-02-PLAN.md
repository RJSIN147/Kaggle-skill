---
phase: 04-kaggle-kernel-execution-gpu-path
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - scripts/convert_notebook.py
  - scripts/push_kernel.py
  - scripts/templates/kernel-metadata.json.tmpl
  - scripts/templates/config.json.tmpl
  - scripts/templates/pyproject.toml.tmpl
autonomous: true
requirements: [EXP-05]
user_setup: []

must_haves:
  truths:
    - "The AI can convert an unchanged experiment.py into experiment.ipynb via `uv run --no-sync jupytext`, non-destructively (a re-convert regenerates from experiment.py and never mutates it)"
    - "The AI can push an experiment to a Kaggle kernel with generated metadata: private, GPU on, internet off by default, competition_sources mechanically from config.json.competition_slug"
    - "The kernel id is the deterministic stable slug <username>/<competition-slug>-exp-NNN, username resolved from `kaggle config view` (no secret echoed)"
    - "The push writes experiments/exp-NNN/kernel_run.json handoff state (slug, code_file, competition, accelerator, effective internet flag, pushed_at, status=PENDING) that poll/pull re-read without re-pushing"
    - "A non-blocking GPU-quota heads-up is surfaced before push and NEVER blocks it (D-13)"
    - "Every kaggle CLI call routes through kaggle_gateway.run_kaggle (timeout-bounded, no-echo, exit-code); no raw status/quota buffer is printed"
  artifacts:
    - path: "scripts/convert_notebook.py"
      provides: "D-02 .py→.ipynb via uv run --no-sync jupytext; non-destructive, re-runnable"
      contains: "--no-sync"
    - path: "scripts/push_kernel.py"
      provides: "metadata gen + quota heads-up + kernels push + kernel_run.json write, all via run_kaggle"
      contains: "run_kaggle"
    - path: "scripts/templates/kernel-metadata.json.tmpl"
      provides: "VERIFIED kernel-metadata schema with enable_internet rendered explicitly"
      contains: "is_private"
    - path: "scripts/templates/config.json.tmpl"
      provides: "config-level kernel.enable_internet toggle defaulting to false (D-06)"
      contains: "enable_internet"
  key_links:
    - from: "scripts/push_kernel.py"
      to: "scripts/kaggle_gateway.py"
      via: "run_kaggle for config view / quota / kernels push; dump_last_error on rc!=0"
      pattern: "run_kaggle"
    - from: "scripts/push_kernel.py"
      to: "experiments/exp-NNN/kernel_run.json"
      via: "write handoff state incl. effective enable_internet (D-06 provenance guard)"
      pattern: "kernel_run"
    - from: "scripts/push_kernel.py"
      to: "scripts/templates/kernel-metadata.json.tmpl"
      via: "_render_text safe_substitute with config slug + resolved username"
      pattern: "kernel-metadata"
---

<objective>
Build the front half of the kernel loop — the thinnest slice that takes the SAME Phase-3
experiment.py from local to a live Kaggle kernel: convert it to a notebook, generate verified
kernel-metadata, surface a non-blocking quota heads-up, push through the gateway, and persist the
push→poll→pull handoff state in experiments/exp-NNN/kernel_run.json. Turns the 04-01
convert + push RED tests GREEN.

Purpose: Realize D-02/D-04/D-05/D-06/D-07/D-13 — a re-runnable, inspectable convert step and a
deterministic-slug, internet-off-by-default, private push whose effective internet value is
recorded as an auditable exception.
Output: `scripts/convert_notebook.py`, `scripts/push_kernel.py`,
`scripts/templates/kernel-metadata.json.tmpl`, config internet toggle, jupytext workspace floor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-CONTEXT.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md
@scripts/run_local.py
@scripts/scaffold_experiment.py
@scripts/kaggle_gateway.py

<interfaces>
<!-- Existing exports the new scripts import. Use directly — no codebase exploration needed. -->

From scripts/kaggle_gateway.py (route EVERY kaggle call through these):
- `run_kaggle(*argv: str, timeout: int = 60) -> tuple[int, str]` — rc==127 CLI missing,
  rc==124 timeout, else (returncode, combined). NEVER print `combined`.
- `dump_last_error(ws: Path, combined: str) -> Path` — quarantine raw output to
  control/raw/last-error.txt (gitignored). Use on any rc!=0 worth keeping.

From scripts/init_workspace.py (import after self-locating sys.path insert, per scaffold_experiment.py:43-52):
- `_render_text(template_name: str, mapping: dict) -> str` — Template(raw).safe_substitute(mapping); use `${KEY}` placeholders.
- `create_if_absent(path: Path, content: str) -> str` — never clobbers.
- `_iso_now() -> str` — UTC ISO-8601 with trailing Z (pushed_at).
- `set_config_field(config_path, key_path: tuple, value) -> int` — fail-clear config-leaf setter.

From scripts/scaffold_experiment.py (copy the hardening patterns):
- `_SLUG_RE = re.compile(r"^[a-z0-9][a-z0-9-]*$")` — a non-empty slug MUST match before it enters the kernel id (block, don't guess).
- `_read_control_json(path) -> dict|None` — fail-clear control-JSON read (corrupt ⇒ message + None, nothing written).

VERIFIED CLI surface (04-RESEARCH.md):
- `kaggle config view` → prints `- username: <name>` (no key). Match that line; never echo the buffer.
- `kaggle quota --format json` → {resource, used, remaining, total, refreshAt} (D-13 heads-up; ignore failure).
- `kaggle kernels push -p <folder>` → folder must contain kernel-metadata.json + code_file (experiment.ipynb).
  Optional `--accelerator NvidiaTeslaT4` (verified) / `NvidiaTeslaP100`; T4×2 string is UNVERIFIED (Assumption A1).
- Convert command: `uv run --no-sync jupytext --to notebook <exp.py> -o <exp.ipynb>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: convert_notebook.py — non-destructive .py→.ipynb via uv run jupytext (D-02)</name>
  <read_first>
    - scripts/run_local.py (the only other `uv run --no-sync` caller — copy docstring posture, uv-absent gate at :106-112, subprocess timeout/OSError handling at :121-134, missing-file fail-clear at :91-97, argparse shape :61-79)
    - scripts/templates/experiment.py.tmpl (the source the convert reuses UNCHANGED — resolve_data_dir seam)
    - tests/test_convert_notebook.py (the RED test this turns GREEN — test_reconvert_idempotent)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (convert_notebook section)
  </read_first>
  <behavior>
    - test_reconvert_idempotent: running convert twice on an unchanged experiment.py regenerates experiment.ipynb both times and leaves experiment.py byte-identical.
    - uv absent on PATH ⇒ exit 1 with a `run \`uv sync\`` remediation; nothing converted.
    - Missing experiments/exp-NNN/experiment.py ⇒ clear message + exit 1.
  </behavior>
  <action>
    Create scripts/convert_notebook.py mirroring run_local.py's portability posture: stdlib-only,
    self-locating via `Path(__file__)`, `--workspace` (default Path.cwd()), `--exp-dir` (required),
    `--timeout` (int, default e.g. 300), argparse-in / exit-code-out, non-interactive.

    Resolve exp_dir = ws / exp_rel; require experiment.py present (else fail-clear + return 1,
    copying run_local.py:91-97). Copy the uv-absent gate verbatim (run_local.py:106-112): if
    `shutil.which("uv") is None` print the `uv sync` remediation and return 1 — NEVER a runtime
    install (CLAUDE.md; this is the load-bearing `--no-sync` posture). Build
    `cmd = ["uv", "run", "--no-sync", "jupytext", "--to", "notebook", str(exp_py), "-o", str(exp_ipynb)]`
    where exp_ipynb = exp_dir / "experiment.ipynb", run with cwd=ws under
    subprocess.run(..., capture_output=True, text=True, timeout=args.timeout) inside the same
    TimeoutExpired / OSError handlers run_local uses. On non-zero exit, surface a short stderr tail
    (never fabricate anything) and return the child code. On success print a next-step line naming
    the produced experiment.ipynb and that push_kernel.py consumes it. D-02 GUARD: the .ipynb is a
    regenerable build artifact — do NOT create_if_absent it (jupytext overwrites the .ipynb each
    run) and NEVER modify experiment.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_convert_notebook.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_convert_notebook.py::test_reconvert_idempotent` passes.
    - Source contains `--no-sync` and does NOT contain `pip install` (grep: `grep -q -- --no-sync scripts/convert_notebook.py && ! grep -q 'pip install' scripts/convert_notebook.py`).
    - Running with an empty PATH-bin (no uv) exits non-zero and prints `uv sync`.
    - experiment.py is byte-identical before/after a convert.
  </acceptance_criteria>
  <done>convert_notebook.py exists, is non-destructive/re-runnable, and the convert test is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: kernel-metadata.json.tmpl + config.json.tmpl internet toggle + jupytext floor</name>
  <read_first>
    - scripts/templates/config.json.tmpl (add the kernel.enable_internet leaf; keep JSON shape)
    - scripts/templates/meta.json.tmpl (the `${KEY}` placeholder convention _render_text fills)
    - scripts/templates/pyproject.toml.tmpl (ML floors block :25-32 — add jupytext floor)
    - tests/fixtures/kernel-metadata.golden.json (the exact schema the template must render to)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (kernel-metadata template + config.json.tmpl sections; Pitfall 4/5 guards)
  </read_first>
  <action>
    Create scripts/templates/kernel-metadata.json.tmpl using `${KEY}` placeholders so
    _render_text/safe_substitute fills them. Body = the VERIFIED schema (04-RESEARCH.md Code
    Examples): id=`${KERNEL_ID}`, title=`${TITLE}`, code_file=`experiment.ipynb`,
    language=`python`, kernel_type=`notebook`, is_private=true, enable_gpu=true, enable_tpu=false,
    enable_internet=`${ENABLE_INTERNET}` (rendered as a bare JSON bool literal — GUARD Pitfall 4:
    the CLI's own `kernels init` writes `true`; we MUST emit an explicit value with false as the
    default), competition_sources=`["${COMPETITION_SLUG}"]`, dataset_sources/kernel_sources/
    model_sources=[]. GUARD Pitfall 5: default enable_gpu:true (guaranteed valid); the exact T4×2
    accelerator string stays out of the template (push_kernel exposes `--accelerator` as an
    override, not a hard default).

    Modify scripts/templates/config.json.tmpl: add a config-level `"kernel": { "enable_internet": false }`
    leaf (D-06). Because init's deep_merge_add_missing only ADDS absent keys, an already-scaffolded
    workspace is retrofitted on the next init merge without clobbering existing values; note this in
    the SUMMARY so 04-05/SKILL documents the setter path (set_config_field on ("kernel","enable_internet")).

    Modify scripts/templates/pyproject.toml.tmpl: add `"jupytext>=1.16"` to the workspace ML
    dependencies list (:25-32), following the existing FLOORS-not-newest-majors comment style. This
    is a DECLARE-in-template step (operator runs `uv sync`); the skill never runtime-installs.
    jupytext is CLAUDE.md-verified stack (Package Legitimacy Audit: Approved), so no legitimacy
    checkpoint is required.
  </action>
  <verify>
    <automated>python3 -c "import re; t=open('scripts/templates/kernel-metadata.json.tmpl').read(); assert '\"is_private\": true' in t and 'enable_internet' in t and '\${ENABLE_INTERNET}' in t and '\${COMPETITION_SLUG}' in t; c=open('scripts/templates/config.json.tmpl').read(); import json; assert 'enable_internet' in c; p=open('scripts/templates/pyproject.toml.tmpl').read(); assert 'jupytext' in p"</automated>
  </verify>
  <acceptance_criteria>
    - kernel-metadata.json.tmpl contains `is_private": true`, `${ENABLE_INTERNET}` (explicit, not hardcoded true), `${COMPETITION_SLUG}`, and the four *_sources arrays.
    - config.json.tmpl parses as valid JSON after placeholder substitution and contains a `kernel.enable_internet` leaf defaulting to false.
    - pyproject.toml.tmpl lists `jupytext>=1.16` in the workspace ML dependencies.
  </acceptance_criteria>
  <done>The template renders to the golden schema, the config toggle exists (default false), and jupytext is declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: push_kernel.py — metadata gen + quota heads-up + push + kernel_run.json</name>
  <read_first>
    - scripts/scaffold_experiment.py (template-render-then-write orchestration, init_workspace import block :43-52, _SLUG_RE hardening :41,149-158, _read_control_json fail-clear :55-73)
    - scripts/kaggle_gateway.py (run_kaggle contract :69-96, dump_last_error :236-250, no-echo module docstring :11-27)
    - scripts/record_experiment.py (canonical JSON write style :339 — json.dumps(obj, indent=2)+"\n")
    - tests/test_push_kernel.py (RED tests: test_metadata_golden, test_internet_provenance)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (push_kernel.py section)
  </read_first>
  <behavior>
    - test_metadata_golden: with a seeded config (slug=titanic) and a mocked `kaggle config view` returning username=testuser, the generated experiments/exp-001/kernel-metadata.json equals kernel-metadata.golden.json.
    - test_internet_provenance: the effective enable_internet value read from config.kernel.enable_internet is written into experiments/exp-001/kernel_run.json.
  </behavior>
  <action>
    Create scripts/push_kernel.py: stdlib-only, self-locating, `--workspace`, `--exp-dir`
    (required), `--accelerator` (optional; verified `NvidiaTeslaT4`/`NvidiaTeslaP100` only — do NOT
    hard-code an unverified T4×2), `--timeout` (int). Import _render_text, create_if_absent,
    _iso_now, set_config_field from init_workspace and run_kaggle, dump_last_error from
    kaggle_gateway (self-locating sys.path insert first).

    Read control/config.json fail-clear (copy _read_control_json): competition_slug (validate with
    _SLUG_RE — block a malformed non-empty slug), and the effective internet flag from
    config["kernel"]["enable_internet"] (default false if absent). Resolve the username via
    run_kaggle("config","view",timeout=30): match the `- username: <name>` line, NEVER echo `out`;
    on rc!=0 or no username line, dump_last_error and fail-clear (return non-zero). Build the kernel
    id ONLY from validated username + slug + exp-NNN (V5: never from Kaggle prose):
    `<username>/<slug>-exp-NNN`.

    Render kernel-metadata.json via _render_text("kernel-metadata.json.tmpl", {KERNEL_ID, TITLE,
    ENABLE_INTERNET rendered as JSON `true`/`false`, COMPETITION_SLUG}) into the exp dir (this is a
    per-push build artifact — write/overwrite it, do not create_if_absent). D-13 non-blocking quota
    heads-up: run_kaggle("quota","--format","json",timeout=30); parse best-effort remaining hours;
    print a short informational note; on ANY failure just skip the note — NEVER block the push.

    Push: run_kaggle("kernels","push","-p",str(exp_dir), [+ "--accelerator", args.accelerator if
    set], timeout=args.timeout). rc==127 ⇒ CLI-missing remediation; rc==124 ⇒ timeout message; any
    rc!=0 ⇒ dump_last_error(ws,out) + fail-closed (never print out). On success, write
    experiments/exp-NNN/kernel_run.json (json.dumps(..., indent=2)+"\n") with keys: exp_id,
    kernel_slug, kernel_version=null, code_file="experiment.ipynb", competition_slug,
    accelerator (the flag value or "enable_gpu"), enable_internet (the EFFECTIVE value — D-06
    guard), pushed_at=_iso_now(), status="PENDING", backend="kernel", docker_image=null,
    machine_shape=null. Print a next-step line pointing at poll_kernel.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_push_kernel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_push_kernel.py::test_metadata_golden` and `::test_internet_provenance` pass.
    - Source routes every kaggle call through `run_kaggle` and never `print`s a raw status/quota/push buffer (grep: source contains `run_kaggle` and `dump_last_error`; no `print(out` / `print(combined`).
    - The kernel id is assembled from `_SLUG_RE`-validated slug + the parsed username only.
    - A quota-call failure does not change the push exit code (heads-up is non-blocking).
    - kernel_run.json contains the effective `enable_internet` field.
  </acceptance_criteria>
  <done>push_kernel.py generates verified metadata, surfaces a non-blocking quota note, pushes via the gateway, writes kernel_run.json, and the push tests are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (skill) → Kaggle API | Push/config/quota calls cross to api.kaggle.com (allowlisted) |
| Kaggle CLI stdout → skill | Untrusted text (username line, quota JSON, push output) — pattern-match only, never echo |
| config.json → kernel metadata | competition_slug is the only variable entering the kernel id; validated by _SLUG_RE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Info-Disclosure | push_kernel CLI output | mitigate | All calls via run_kaggle (no-echo); raw output quarantined to control/raw/last-error.txt via dump_last_error; never printed |
| T-4-03 | Tampering | enable_internet flag | mitigate | Template renders enable_internet explicitly false by default; effective value recorded in kernel_run.json as an auditable exception (D-06) |
| T-4-04 | Tampering/EoP | kernel id construction | mitigate | id built only from _SLUG_RE-validated slug + parsed `config view` username; never derived from competition/kernel prose (V5) |
| T-4-05 | Info-Disclosure | off-allowlist egress on push | accept | api.kaggle.com + storage.googleapis.com already allowlisted (04-RESEARCH A5); no new host introduced |
| T-4-SC | Tampering | jupytext workspace dep | mitigate | jupytext declared in pyproject template only (never runtime-installed); CLAUDE.md-verified stack, Package Legitimacy Audit = Approved — no [ASSUMED]/[SUS] gate needed |
</threat_model>

<verification>
- `uv run pytest tests/test_convert_notebook.py tests/test_push_kernel.py -x -q` GREEN.
- `uv run pytest -q` full suite green (no regression).
- Template + config + pyproject assertions (Task 2 automated verify) pass.
</verification>

<success_criteria>
- The same Phase-3 experiment.py converts to a notebook and pushes to a private, GPU-on,
  internet-off kernel with a deterministic slug and mechanically-populated competition_sources.
- kernel_run.json handoff state is written with the effective internet flag recorded.
- A non-blocking quota heads-up appears; the push never blocks on it.
- Every kaggle call routes through the gateway; no raw buffer is echoed.
</success_criteria>

<output>
Create `.planning/phases/04-kaggle-kernel-execution-gpu-path/04-02-SUMMARY.md` when done.
Document the exact kernel_run.json schema so 04-03 (poll/pull) and 04-04 (recorder) consume it.
</output>
