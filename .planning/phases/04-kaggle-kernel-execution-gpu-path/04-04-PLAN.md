---
phase: 04-kaggle-kernel-execution-gpu-path
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - scripts/record_experiment.py
autonomous: true
requirements: [EXP-05]
user_setup: []

must_haves:
  truths:
    - "A kernel that reports COMPLETE but actually threw is recorded FAILED(kernel_error) — even when a valid result.json exists — because the log is scanned BEFORE result.json is trusted"
    - "The silent-failure scan is a NEW FIRST RUNG inside the existing record_experiment.py D-06 ladder, gated on --kernel-log (kernel path only); the local path is byte-for-byte unchanged"
    - "The FAILURE_REASONS enum gains exactly ONE new reason (kernel_error); missing/invalid kernel result.json still maps to the EXISTING missing_result/schema_invalid reasons (no duplication)"
    - "A clean kernel log + a valid result.json is recorded SUCCESS via the unchanged downstream ladder"
    - "Kernel provenance (backend, slug, effective internet, image) is merged from kernel_run.json into meta.json so an internet-on kernel run is an auditable exception"
    - "The log scan is pure pattern-match — no executed path/command is derived from log content, and the raw log is never echoed"
  artifacts:
    - path: "scripts/record_experiment.py"
      provides: "extended recorder: scan_kernel_log first rung + kernel_error reason + --kernel-log flag + kernel provenance merge"
      contains: "kernel_error"
  key_links:
    - from: "scripts/record_experiment.py"
      to: "experiments/exp-NNN/kernel_log.txt"
      via: "--kernel-log path scanned for D-11 markers BEFORE result.json is read"
      pattern: "scan_kernel_log"
    - from: "scripts/record_experiment.py"
      to: "experiments/exp-NNN/kernel_run.json"
      via: "merge backend/slug/effective-internet/image provenance into meta.json"
      pattern: "kernel_run"
---

<objective>
The headline correctness property of Phase 4: extend the ONE anti-lie recorder so a kernel that
reports COMPLETE but actually threw is recorded FAILED — never a success — even if a valid
result.json came back. Per D-12 this is a NEW FIRST RUNG of the existing D-06 fail-closed ladder
(kernel path only), adding exactly one reason (kernel_error) and reusing the rest of the enum. One
recorder, one ladder: the literal realization of "Phase 4 extends the contract, never re-derives
it." Turns the 04-01 recorder RED tests GREEN. Fixture-driven — no live GPU required.

Purpose: Realize D-11 (traceback + error/OOM marker scan) and D-12 (single-recorder extension) and
the D-06 internet-effective provenance guard, without forking a second recorder or proliferating
kernel-specific reasons.
Output: extended `scripts/record_experiment.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-CONTEXT.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-02-SUMMARY.md
@scripts/record_experiment.py

<interfaces>
<!-- record_experiment.py current shape (extend in place — do NOT fork): -->
- `FAILURE_REASONS = ("missing_result", "schema_invalid", "non_finite", "out_of_range")` (:59) → add `"kernel_error"`.
- `_read_json(path) -> (parsed, error)` (:67-78) — reuse for kernel_run.json read.
- `build_parser()` (:212-228) has `--run-exit-code` (:225-227) — add `--kernel-log` alongside it.
- `main()` classification block (:278-294): `run_failed` pre-classification precedes the
  `_read_json(result_path)` + `_validate_result` ladder. The kernel-log scan becomes the FIRST rung
  BEFORE `result.json` is read.
- Downstream (`_build_provenance`, meta merge :301-336, `rebuild_ledger_file`, `_stage_provenance`,
  VERDICT stub) is REUSED UNTOUCHED.

D-11 marker set (04-RESEARCH.md Code Examples — pure pattern-match, never echo the log):
`Traceback (most recent call last)`, `\nError:`, `\nException:`,
`Your notebook tried to allocate more memory than is available`, `Killed`, `Notebook Exceeded`.
Assumption A3: the log may be plain text OR a JSON array of {stream_name,data}; scan parse-JSON-
if-possible-then-concatenate-data, else scan as text; finalize the marker set at the live checkpoint.

kernel_run.json (from 04-02): {exp_id, kernel_slug, competition_slug, accelerator,
enable_internet (effective), backend="kernel", docker_image, machine_shape, ...}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scan_kernel_log first rung + kernel_error reason + --kernel-log flag</name>
  <read_first>
    - scripts/record_experiment.py (WHOLE file — FAILURE_REASONS :59, _read_json :67-78, _validate_result :103-158, build_parser :212-228, main classification :276-294, meta merge :301-336, VERDICT/ledger/stage :338-372)
    - tests/test_record_kernel.py (RED: test_traceback_beats_valid_result, test_clean_log_success, test_oom_marker)
    - tests/test_record_experiment.py (existing recorder tests — MUST stay GREEN; reuse its seeding helpers)
    - tests/fixtures/kernel_logs/*.txt (the logs the scan classifies)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (record_experiment.py section — exact wiring instructions)
  </read_first>
  <behavior>
    - test_traceback_beats_valid_result: a VALID result.json present AND --kernel-log → complete_but_threw.txt ⇒ meta.status=FAILED, failure_reason=kernel_error (log scanned BEFORE result.json is read).
    - test_clean_log_success: clean.txt + valid result.json ⇒ status=SUCCESS via the unchanged ladder.
    - test_oom_marker: oom.txt ⇒ FAILED(kernel_error).
    - Local path (no --kernel-log) is byte-for-byte unchanged: all existing test_record_experiment.py tests stay GREEN.
  </behavior>
  <action>
    Extend scripts/record_experiment.py IN PLACE (do NOT fork a second recorder — D-12).

    (1) Append exactly one reason to the enum: FAILURE_REASONS gains `"kernel_error"` (:59). Add NO
    other kernel-specific reasons — missing/invalid kernel result.json keeps mapping to the EXISTING
    missing_result/schema_invalid.

    (2) Add a module-level marker tuple `_KERNEL_ERROR_MARKERS` (the six D-11 markers from
    <interfaces>) and a helper `scan_kernel_log(log_text: str) -> bool` returning
    `any(marker in log_text for marker in _KERNEL_ERROR_MARKERS)` — pure pattern-match, NEVER echo
    the log, NEVER derive an executed path/command from its content (V5/V7). Handle Assumption A3:
    try json.loads(log_text); if it is a list of {stream_name,data} dicts, concatenate the `data`
    fields and scan that; otherwise scan the raw text.

    (3) Add `--kernel-log` (optional path) to build_parser alongside `--run-exit-code` (:225-227).
    Absent ⇒ existing local behavior unchanged.

    (4) Wire the NEW FIRST RUNG in main()'s classification block (:278-294), kernel path only: if
    --kernel-log is provided, read that file (fail-clear) and run scan_kernel_log FIRST; on a hit set
    status="FAILED", failure_reason="kernel_error", valid_result=None BEFORE result.json is read.
    Otherwise fall through to the EXISTING run_failed / _read_json(result_path) / _validate_result
    ladder completely unchanged. Everything downstream (_build_provenance, meta merge, VERDICT stub,
    rebuild_ledger_file, _stage_provenance) is reused untouched.

    (5) Kernel provenance: when --kernel-log is set, read experiments/exp-NNN/kernel_run.json
    fail-clear and merge a kernel provenance block into meta (backend="kernel", kernel_slug,
    enable_internet effective — D-06 auditable exception, docker_image/machine_shape when present).
    Thread it the way --run-exit-code pre-classification already threads through; do not disturb the
    local meta shape when --kernel-log is absent.
  </action>
  <verify>
    <automated>uv run pytest tests/test_record_kernel.py tests/test_record_experiment.py -x -q; python3 -c "s=open('scripts/record_experiment.py').read(); assert s.count('kernel_error')>=1 and 'scan_kernel_log' in s and '--kernel-log' in s; import re; assert 'kernel_error' in re.search(r'FAILURE_REASONS\\s*=\\s*\\(([^)]*)\\)', s).group(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_record_kernel.py::test_traceback_beats_valid_result`, `::test_clean_log_success`, `::test_oom_marker` all pass.
    - ALL existing `tests/test_record_experiment.py` tests remain GREEN (local path unchanged).
    - FAILURE_REASONS contains exactly ONE new reason `kernel_error` (no kernel_traceback/kernel_timeout/… proliferation).
    - Source contains `scan_kernel_log` and `--kernel-log`; the scan derives no executed path/command and never prints the log.
    - meta.json for a kernel run carries the effective enable_internet from kernel_run.json.
  </acceptance_criteria>
  <done>The recorder scans the kernel log as its first rung (traceback/OOM ⇒ kernel_error before result.json is trusted), the local path is unchanged, and both recorder test suites are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel_log.txt → recorder | Untrusted Kaggle log text enters by PATH; scanned for markers only, never executed |
| kernel_run.json → meta.json | Provenance merge; effective internet flag surfaced as an auditable field |
| meta.json → git | Provenance staged by explicit path (existing _stage_provenance), never blanket add |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Tampering/EoP | scan_kernel_log | mitigate | Pure `marker in text` pattern-match; no path/command/URL derived from log content; raw log never echoed (V5/V7) |
| T-4-03 | Tampering | enable_internet provenance | mitigate | Effective internet value merged from kernel_run.json into meta.json so an internet-on run is a visible, auditable exception (D-06) |
| T-4-07 | Spoofing | COMPLETE-but-threw success | mitigate | Log scanned BEFORE result.json is trusted; a traceback/OOM hit ⇒ FAILED(kernel_error) even with a valid result.json (the headline property, Success Criterion 3) |
| T-4-06 | Info-Disclosure | provenance staging | mitigate | Reuses existing explicit-path `_stage_provenance` (never blanket `git add`) — control/raw/ stays unstaged (V12) |
| T-4-SC | Tampering | package installs | mitigate | No package install (in-place stdlib edit only) |
</threat_model>

<verification>
- `uv run pytest tests/test_record_kernel.py -x -q` GREEN (traceback beats result, clean success, OOM).
- `uv run pytest tests/test_record_experiment.py -x -q` GREEN (local path unchanged — no regression).
- `uv run pytest -q` full suite green.
- Source assertion (Task 1 automated verify): exactly one new enum reason, scan helper + flag present.
</verification>

<success_criteria>
- Success Criterion 3 is met: a COMPLETE-but-threw kernel (valid result.json present) is recorded
  FAILED(kernel_error), because the log is scanned before result.json is trusted.
- The contract is EXTENDED, not re-derived: one recorder, one ladder, exactly one new reason, local
  path byte-for-byte unchanged.
- Kernel provenance (backend, slug, effective internet, image) lands in meta.json.
</success_criteria>

<output>
Create `.planning/phases/04-kaggle-kernel-execution-gpu-path/04-04-SUMMARY.md` when done.
</output>
