---
phase: 04-kaggle-kernel-execution-gpu-path
plan: 05
type: execute
wave: 4
depends_on: [04-03, 04-04]
files_modified:
  - SKILL.md
autonomous: false
requirements: [EXP-05]
user_setup:
  - service: kaggle
    why: "The one opt-in live-push confirms the T4×2 accelerator string, the real kernel-log format, and the status render against live Kaggle GPU compute"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "SKILL.md documents the kernel path as discrete, re-runnable steps convert → push → poll → pull → record, each `python3 scripts/<x>.py --workspace <cwd> --exp-dir experiments/exp-NNN`"
    - "SKILL.md states that after an interrupted/detached poll the user re-runs poll (then pull) WITHOUT re-pushing — GPU time already spent is never re-burned (D-01/D-09)"
    - "SKILL.md surfaces the non-blocking GPU-quota heads-up (D-13) and the internet-off-by-default + effective-value-recorded note (D-06)"
    - "The scripts-table gains rows for convert_notebook / push_kernel / poll_kernel / pull_kernel in the established style, and record_experiment's row notes the kernel-path --kernel-log scan"
    - "One opt-in live push runs the full push→poll→pull→record loop against a real kernel and confirms the three live-only unknowns, feeding findings back into references/kaggle-cli-behavior.md and the T4×2 default"
  artifacts:
    - path: "SKILL.md"
      provides: "kernel-path sequencing section + scripts-table rows + D-13/D-06 notes"
      contains: "poll_kernel"
  key_links:
    - from: "SKILL.md"
      to: "scripts/poll_kernel.py"
      via: "kernel-path sequencing documents the detach/resume loop the SKILL holds"
      pattern: "poll_kernel"
---

<objective>
Wire the kernel path into SKILL.md so the AI can drive convert → push → poll → pull → record as
discrete, resumable steps (the SKILL holds the human/AI loop between poll re-runs), then run the
single opt-in live push that resolves the three genuinely live-only unknowns (T4×2 accelerator
string, exact kernel-log format, status render). This is the only non-autonomous plan in the phase:
the live push is one explicit human-verify task, NOT a phase-wide blocker — the entire default
suite is already green from fixtures before this runs.

Purpose: Complete EXP-05's user-facing sequencing (D-01 discrete/resumable steps, D-13 quota note,
D-06 internet note) and close Assumptions A1/A2/A3 with one deliberate GPU spend.
Output: extended `SKILL.md`; verified live findings recorded in references/kaggle-cli-behavior.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-CONTEXT.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-02-SUMMARY.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-03-SUMMARY.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-04-SUMMARY.md
@SKILL.md

<interfaces>
<!-- SKILL.md anchors to extend (do not restructure the doc): -->
- "Local experiment loop (EXP-*/MEM-*)" section (:198-266) — mirror its numbered-step, reserved-exit-code, SKILL-holds-the-loop style for the new kernel section.
- Gate protocol / reserved-exit-code prose (:167-176) — reuse conventions for the detach/resume exit codes.
- "Scripts (progressive disclosure)" table (:281-298) — add four rows in the `| scripts/x.py | EXP-05/D-0N one-line purpose |` style; note record_experiment's --kernel-log kernel-path scan.

Kernel step invocations (from 04-02/03/04 SUMMARYs):
- `python3 scripts/convert_notebook.py --workspace <cwd> --exp-dir experiments/exp-NNN`
- `python3 scripts/push_kernel.py    --workspace <cwd> --exp-dir experiments/exp-NNN [--accelerator NvidiaTeslaT4]`
- `python3 scripts/poll_kernel.py    --workspace <cwd> --exp-dir experiments/exp-NNN [--budget <sec>]`  (re-run to reattach after DETACH)
- `python3 scripts/pull_kernel.py    --workspace <cwd> --exp-dir experiments/exp-NNN`
- `python3 scripts/record_experiment.py --workspace <cwd> --exp-dir experiments/exp-NNN --kernel-log experiments/exp-NNN/kernel_log.txt`

Live-only unknowns to confirm (04-RESEARCH Assumptions A1/A2/A3): exact T4×2 accelerator string; kernel-log plain-text-vs-JSON shape + final marker set; status render `KernelWorkerStatus.<NAME>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md kernel-path sequencing + scripts-table rows + D-13/D-06 notes</name>
  <read_first>
    - SKILL.md (WHOLE local-loop section :198-266, gate-protocol :167-176, scripts table :281-298)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md (exact flags, exit-code semantics, kernel_run.json + kernel_log.txt locations)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (SKILL.md section)
  </read_first>
  <action>
    Add a "Kaggle kernel loop (EXP-05, GPU path)" section to SKILL.md, placed after the local-loop
    section, mirroring its numbered-step style and the D-02 "separate idempotent entry points the
    SKILL sequences; argparse in / exit code out; the SKILL holds the human/AI loop" framing.
    Document the discrete sequence with the exact invocations from <interfaces>:
    0. When execution_target is kernel (or an operator opts into a kernel run), start from a
       scaffolded experiment; note the same experiment.py runs unchanged (resolve_data_dir auto-
       selects /kaggle/input on the kernel).
    1. convert (build the inspectable experiment.ipynb — regenerable from experiment.py, D-02).
    2. push (generates metadata, surfaces the non-blocking D-13 quota heads-up, writes kernel_run.json;
       note internet is OFF by default and the effective value is recorded in provenance — D-06;
       note the deterministic <username>/<slug>-exp-NNN slug re-pushes the SAME kernel).
    3. poll (bounded backoff; on our-side timeout it DETACHES — the SKILL instructs the user to
       simply re-run poll later to reattach, then pull, WITHOUT re-pushing / re-burning GPU time —
       D-01/D-09). State the reserved detach exit code semantics per the gate-protocol conventions.
    4. pull (fetches result.json + artifacts + kernel_log.txt + image provenance).
    5. record with `--kernel-log experiments/exp-NNN/kernel_log.txt` — the recorder scans the log
       FIRST: a traceback/OOM ⇒ FAILED(kernel_error) even if status said COMPLETE and a valid
       result.json exists (the anti-silent-failure guarantee, Success Criterion 3). Then the SAME
       regen_strategy step as the local loop.

    Add four rows to the "Scripts (progressive disclosure)" table for convert_notebook / push_kernel /
    poll_kernel / pull_kernel in the established one-line style with EXP-05/D-0N tags, and amend the
    record_experiment.py row to note the kernel-path `--kernel-log` scan (+kernel_error reason). Do
    NOT restructure the doc or touch unrelated sections. Keep SKILL.md lean (progressive-disclosure
    cost); push deep detail to the plan/references, not the body.
  </action>
  <verify>
    <automated>python3 -c "s=open('SKILL.md').read(); assert all(k in s for k in ['convert_notebook','push_kernel','poll_kernel','pull_kernel','--kernel-log','kernel_error']); assert 'without re-pushing' in s.lower() or 'without re-push' in s.lower()"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md documents the convert → push → poll → pull → record sequence with the exact invocations, and the detach/resume (re-run poll without re-pushing) instruction.
    - The scripts table has rows for all four new scripts; record_experiment's row notes `--kernel-log` + kernel_error.
    - The D-13 non-blocking quota note and D-06 internet-off/effective-value note both appear.
    - No unrelated SKILL.md section is restructured.
  </acceptance_criteria>
  <done>SKILL.md sequences the kernel path as discrete resumable steps with the quota + internet notes and the four new script rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: One opt-in live push — confirm T4×2 string, log format, status render (A1/A2/A3)</name>
  <what-built>
    The full kernel loop is built and the entire default (mocked) test suite is green: convert_notebook,
    push_kernel (metadata gen + non-blocking quota + push + kernel_run.json), poll_kernel (enum-parsed
    bounded backoff + detach-not-cancel), pull_kernel (output + logs + image provenance), and the
    record_experiment.py silent-failure rung — all wired into SKILL.md. What CANNOT be proven without a
    real GPU push are four live-only unknowns (04-RESEARCH Assumptions A1/A2/A3 + A4): the exact T4×2
    accelerator string, the real kernel-log format + final marker set, the exact status render, and
    the exact `kernels push` output version string (A4) that push_kernel.py parses into kernel_version.
  </what-built>
  <how-to-verify>
    This is the ONE opt-in live push (it spends a slice of the 30h/week GPU quota). Only run it when you
    want to confirm the live surface — the phase is otherwise complete and green from fixtures.

    1. Ensure Kaggle creds are validated (Phase 1) and a competition is captured with data (Phase 2),
       and one experiment is scaffolded (Phase 3): `experiments/exp-001/experiment.py` exists.
    2. Run the loop against a real kernel:
       - `python3 scripts/convert_notebook.py --workspace $(pwd) --exp-dir experiments/exp-001`
       - `python3 scripts/push_kernel.py --workspace $(pwd) --exp-dir experiments/exp-001` (optionally add `--accelerator NvidiaTeslaT4`)
       - `python3 scripts/poll_kernel.py --workspace $(pwd) --exp-dir experiments/exp-001`
       - `python3 scripts/pull_kernel.py --workspace $(pwd) --exp-dir experiments/exp-001`
       - `python3 scripts/record_experiment.py --workspace $(pwd) --exp-dir experiments/exp-001 --kernel-log experiments/exp-001/kernel_log.txt`
       Or run the live integration test: `uv run pytest -m live --run-live tests/test_kernel_live.py`.
    3. Confirm and CAPTURE into references/kaggle-cli-behavior.md:
       (a) the exact multi-GPU accelerator string (A1) — if confirmed, set it as the D-04 default;
       (b) the real `kaggle kernels logs` shape (plain text vs JSON) + whether the current
           `_KERNEL_ERROR_MARKERS` set catches a genuine traceback (A3) — adjust the marker set if needed;
       (c) the exact `kaggle kernels status` render vs the `_STATUS_RE` regex (A2);
       (d) the exact `kaggle kernels push` output version string vs push_kernel.py's version regex (A4)
           — confirm kernel_run.json.kernel_version captured the real auto-assigned version integer;
           adjust the regex if the output format differs.
       Expected outcome: a completed kernel run recorded to the ledger with kernel provenance; a
       deliberately-throwing kernel recorded FAILED(kernel_error) despite a COMPLETE status.
  </how-to-verify>
  <resume-signal>Type "approved" (with the confirmed T4×2 string / log shape / status render captured to references/kaggle-cli-behavior.md), or describe what diverged so the parser/marker set/default is adjusted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Kaggle kernel → skill | The one live push exercises the real untrusted-text + egress paths the mocked suite simulated |
| operator → GPU quota | The live push deliberately spends a slice of the 30h/week quota (opt-in, gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Info-Disclosure | live status/log output | mitigate | The live run still routes through run_kaggle (no-echo); the log is written to a file, never streamed to the terminal |
| T-4-03 | Tampering | live internet flag | mitigate | The live push uses the internet-off default; the effective value is recorded in kernel_run.json/meta.json as an auditable exception (D-06) |
| T-4-08 | DoS/Resource | GPU quota spend | accept | Single opt-in push, human-gated; D-13 heads-up shown; full quota gating is deferred to Phase 5 by design |
| T-4-SC | Tampering | package installs | mitigate | No package install (SKILL.md doc edit + a gated live run only) |
</threat_model>

<verification>
- Task 1 automated verify passes (kernel-path sequencing + table rows + notes present in SKILL.md).
- `uv run pytest -q` full mocked suite green BEFORE the checkpoint (the phase does not depend on the live run).
- Checkpoint: the live loop records a completed kernel run and a throwing kernel as FAILED(kernel_error); the three live unknowns are captured into references/kaggle-cli-behavior.md.
</verification>

<success_criteria>
- SKILL.md sequences the discrete, resumable kernel loop with the detach/resume, quota, and internet notes.
- The one opt-in live push confirms the T4×2 string, the real log format/marker set, and the status render — or surfaces exactly what to adjust.
- The full phase is green from fixtures independent of whether the live push is run.
</success_criteria>

<output>
Create `.planning/phases/04-kaggle-kernel-execution-gpu-path/04-05-SUMMARY.md` when done.
Record the live findings (T4×2 string / log shape / status render) in references/kaggle-cli-behavior.md.
</output>
