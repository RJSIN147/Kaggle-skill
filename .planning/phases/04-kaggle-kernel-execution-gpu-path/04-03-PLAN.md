---
phase: 04-kaggle-kernel-execution-gpu-path
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - scripts/poll_kernel.py
  - scripts/pull_kernel.py
  - scripts/templates/gitignore.tmpl
autonomous: true
requirements: [EXP-05]
user_setup: []

must_haves:
  truths:
    - "The AI can poll a pushed kernel to a terminal KernelWorkerStatus by parsing the `has status \"…\"` line against the authoritative enum (no --format json on status)"
    - "Polling backs off exponentially with a cap + jitter over a configurable ~2h wall-clock budget — no 429 storm — and tolerates transient CLI errors up to a threshold, never misreading a blip as kernel death"
    - "On our-side budget expiry with the kernel still in-flight the poller DETACHES (writes kernel_run.json status PENDING/DETACHED) and never issues a cancel — a re-run of poll reattaches"
    - "The AI can pull kernel output (result.json + artifacts + rendered .ipynb) and the run log into experiments/exp-NNN, reusing the same artifacts/ + result.json contract the local runner uses"
    - "Image/version provenance (docker_image + machine_shape) is captured via `kernels pull -m` and merged into kernel_run.json (D-14 record-don't-pin)"
    - "The pulled run log is written to kernel_log.txt and its PATH is handed to the recorder; it is never echoed and no executed path/command is derived from its content"
    - "The workspace gitignore ignores pulled kernel artifacts + kernel_log.txt while keeping kernel_run.json tracked"
  artifacts:
    - path: "scripts/poll_kernel.py"
      provides: "status-enum classify + exponential backoff/jitter/budget + detach-not-cancel"
      contains: "KernelWorkerStatus"
    - path: "scripts/pull_kernel.py"
      provides: "kernels output + logs + pull -m image provenance merge, all via run_kaggle"
      contains: "kernel_log"
    - path: "scripts/templates/gitignore.tmpl"
      provides: "ignore kernel artifacts + kernel_log.txt; keep kernel_run.json tracked"
      contains: "kernel_run.json"
  key_links:
    - from: "scripts/poll_kernel.py"
      to: "experiments/exp-NNN/kernel_run.json"
      via: "read slug/version handoff; write DETACHED/terminal status back (never re-push)"
      pattern: "kernel_run"
    - from: "scripts/pull_kernel.py"
      to: "experiments/exp-NNN/kernel_log.txt"
      via: "write the pulled log to a file; hand the path to record_experiment.py --kernel-log"
      pattern: "kernel_log"
    - from: "scripts/pull_kernel.py"
      to: "scripts/kaggle_gateway.py"
      via: "run_kaggle for kernels output/logs/pull -m; dump_last_error on failure"
      pattern: "run_kaggle"
---

<objective>
Build the middle of the kernel loop — poll a pushed kernel to completion under a bounded,
backed-off, 429-safe loop that detaches (never cancels) on our-side timeout, then pull the same
result.json + artifacts contract the local runner uses plus the run log and image provenance.
Turns the 04-01 poll RED tests GREEN and stages kernel_log.txt for the recorder's silent-failure
scan. Reads the kernel_run.json handoff written by 04-02 — never re-pushes.

Purpose: Realize D-08/D-09/D-10 (bounded exponential backoff, detach-not-cancel, transient-error
tolerance) and D-14 (image provenance record-don't-pin), routing every call through the gateway.
Output: `scripts/poll_kernel.py`, `scripts/pull_kernel.py`, gitignore kernel-artifact patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-CONTEXT.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-02-SUMMARY.md
@scripts/run_local.py
@scripts/download_data.py
@scripts/kaggle_gateway.py

<interfaces>
<!-- kernel_run.json schema (written by 04-02 push_kernel.py — consume it, do not redefine): -->
<!-- {exp_id, kernel_slug, kernel_version, code_file, competition_slug, accelerator,       -->
<!--  enable_internet (effective), pushed_at, status="PENDING", backend="kernel",          -->
<!--  docker_image=null, machine_shape=null} — poll/pull re-read kernel_slug, never re-push. -->

From scripts/kaggle_gateway.py:
- `run_kaggle(*argv, timeout=60) -> (rc, combined)` — rc==127 CLI missing, rc==124 timeout. Never print combined.
- `dump_last_error(ws, combined) -> Path` — quarantine raw output on persistent failure.

Status parse contract (04-RESEARCH.md Code Examples — VERIFIED enum; copy exactly):
- TERMINAL = {"COMPLETE", "ERROR", "CANCEL_ACKNOWLEDGED"}
- IN_FLIGHT = {"QUEUED", "RUNNING", "NEW_SCRIPT", "CANCEL_REQUESTED"}
- `_STATUS_RE = re.compile(r'status\s+"(?:KernelWorkerStatus\.)?([A-Z_]+)"')`; classify_status
  returns the token or None (None ⇒ transient/unparseable ⇒ retry, D-10). Regex tolerates both
  `KernelWorkerStatus.NAME` and bare `NAME` (Assumption A2). `kaggle kernels status` has NO --format json.

VERIFIED pull surface (04-RESEARCH.md):
- `kaggle kernels output <slug> -p <exp-dir> --force` → result.json, oof.npy, rendered .ipynb (flat, auto-paged).
- `kaggle kernels logs <slug>` → the full execution log string → write to exp-dir/kernel_log.txt (never echo).
- `kaggle kernels pull <slug> -m -p <tmp>` → regenerated metadata incl. docker_image (gcr.io/…@sha256:) + machine_shape → merge into kernel_run.json.

From scripts/download_data.py: the CLI→files-on-disk posture (do NOT copy its zip-slip/unzip logic — kernel output is flat, no zip).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll_kernel.py — enum classify + exponential backoff/budget + detach-not-cancel</name>
  <read_first>
    - scripts/run_local.py (timeout-bounded "our patience, never a hang" posture; argparse shape; fail-clear reads)
    - scripts/kaggle_gateway.py (run_kaggle :69-96, dump_last_error :236-250)
    - scripts/record_experiment.py (_read_json fail-clear :67-78 — reuse for kernel_run.json read)
    - tests/test_poll_kernel.py (RED: test_status_classify, test_backoff_budget, test_detach_not_cancel)
    - tests/fixtures/status/*.txt (the status render the parser must handle)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (poll_kernel.py section; Anti-Patterns — no case-insensitive grep)
  </read_first>
  <behavior>
    - test_status_classify: each status fixture maps to its correct in-flight/terminal bucket; a `Failure message:` body never yields a false STATUS token.
    - test_backoff_budget: with a seeded RNG and a mocked/monotonic clock, intervals are exponential, capped, jittered; the loop stops at the wall-clock budget; transient run_kaggle errors (rc!=0 or classify→None) are tolerated up to a consecutive-failure threshold, then fail-closed.
    - test_detach_not_cancel: run_kaggle mocked to stay RUNNING past the budget ⇒ kernel_run.json.status becomes PENDING/DETACHED and NO cancel-style argv is ever passed to run_kaggle.
  </behavior>
  <action>
    Create scripts/poll_kernel.py: stdlib-only (argparse, json, re, time, random), self-locating,
    `--workspace`, `--exp-dir` (required), `--budget` (seconds, default ~7200 for ~2h),
    `--poll-timeout` (per-call run_kaggle timeout, default 60). Import run_kaggle, dump_last_error.

    Read experiments/exp-NNN/kernel_run.json fail-clear (reuse the _read_json posture); re-derive
    kernel_slug from it — NEVER re-push and never re-derive the slug from config. Copy the VERIFIED
    enum parser from <interfaces> verbatim (TERMINAL/IN_FLIGHT sets + _STATUS_RE + classify_status)
    — NOT a case-insensitive grep (the shepsci anti-pattern).

    Bounded loop keyed on `time.monotonic()` vs the budget. Each iteration:
    run_kaggle("kernels","status",slug,timeout=args.poll_timeout); if rc!=0 OR classify_status(out)
    is None, treat as a TRANSIENT error — increment a consecutive-failure counter, back off, retry;
    reset the counter on any successful parse (Pitfall 3: a single blip is never kernel death).
    Fail-closed (dump_last_error + non-zero exit) only when consecutive failures exceed a threshold
    (pick a concrete value, e.g. 5) OR the budget expires while never reaching terminal. On a
    TERMINAL token: write it into kernel_run.json.status and return 0 (COMPLETE) / a distinct
    non-zero for ERROR/CANCEL_ACKNOWLEDGED, printing a next-step pointing at pull_kernel.py.

    Backoff = exponential with cap + jitter (D-10): start ~10s, multiply (~2x) to a cap (~60-120s),
    add small random jitter via random.uniform; sleep between polls. Pick concrete constants and
    document them. DETACH-not-cancel (D-09): on budget expiry with an IN_FLIGHT status, write
    kernel_run.json.status="DETACHED" (json.dumps(...,indent=2)+"\n") and return a DISTINCT exit
    code — NEVER issue a `kernels cancel`. Re-running poll reattaches. Never echo the status/failure
    buffer; on persistent failure dump_last_error(ws,out).
  </action>
  <verify>
    <automated>uv run pytest tests/test_poll_kernel.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_poll_kernel.py::test_status_classify`, `::test_backoff_budget`, `::test_detach_not_cancel` pass.
    - Source contains the exact TERMINAL/IN_FLIGHT enum sets and `_STATUS_RE`; contains no case-insensitive `.lower()`/`in out` substring status matching.
    - Source never issues a cancel argv and never `print`s the status buffer; uses `time.monotonic()` + `random` for backoff/jitter.
    - On budget expiry with IN_FLIGHT, kernel_run.json.status is DETACHED and the exit code is distinct from the terminal codes.
  </acceptance_criteria>
  <done>poll_kernel.py bounds-polls with 429-safe backoff, detaches (never cancels) on timeout, and the poll tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pull_kernel.py — output + logs + image provenance; gitignore kernel artifacts</name>
  <read_first>
    - scripts/download_data.py (CLI→files-on-disk posture via run_kaggle; do NOT copy unzip/zip-slip logic)
    - scripts/kaggle_gateway.py (run_kaggle, dump_last_error, _append_line_if_absent :218-233 for runtime gitignore retrofit)
    - scripts/record_experiment.py (canonical JSON write :339; _read_json :67-78 for kernel_run.json merge)
    - scripts/templates/gitignore.tmpl (the Phase-3 experiment-artifacts block :20-29 to extend)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-PATTERNS.md (pull_kernel.py + gitignore.tmpl sections)
  </read_first>
  <behavior>
    - After a mocked `kernels output`, result.json + artifacts land under experiments/exp-NNN (same contract the local runner uses).
    - The pulled log is written to experiments/exp-NNN/kernel_log.txt and is never printed to stdout.
    - docker_image + machine_shape from `kernels pull -m` are merged into kernel_run.json, preserving existing keys.
  </behavior>
  <action>
    Create scripts/pull_kernel.py: stdlib-only, self-locating, `--workspace`, `--exp-dir`
    (required), `--timeout`. Import run_kaggle, dump_last_error and the canonical JSON helpers.
    Read kernel_run.json fail-clear for kernel_slug (never re-derive).

    Three gateway calls, each via run_kaggle: (1) output —
    run_kaggle("kernels","output",slug,"-p",str(exp_dir),"--force",timeout=…): pulls result.json,
    oof.npy, rendered .ipynb as FLAT files (no --unzip — kernel output is not a zip; do NOT import
    download_data's extraction). (2) logs — run_kaggle("kernels","logs",slug,…): capture the log
    string from `out` and write it to exp_dir/kernel_log.txt; NEVER echo it (Kaggle-sourced
    untrusted text, possibly large/token-shaped — V5/V7). (3) image provenance —
    run_kaggle("kernels","pull",slug,"-m","-p",str(tmp_meta),…) into a temp dir; read the
    regenerated metadata's docker_image + machine_shape and MERGE them into kernel_run.json
    (read→update dict→json.dumps(...,indent=2)+"\n"), preserving all existing keys (D-14
    record-don't-pin). On any rc!=0 for a required step: rc==127 CLI-missing / rc==124 timeout
    messages, else dump_last_error(ws,out) + fail-closed; never print the raw buffer. Hand the
    kernel_log.txt path forward in the final next-step print (record_experiment.py --kernel-log).

    Modify scripts/templates/gitignore.tmpl: in the Phase-3 experiment-artifacts block, add an
    ignore for `experiments/*/kernel_log.txt` and confirm pulled artifacts are covered by the
    existing `experiments/*/artifacts/` + `experiments/*/*.csv|*.pkl|…` patterns; add
    `!experiments/*/kernel_run.json` to KEEP the small provenance file TRACKED (D-03). NOTE this is
    the WORKSPACE gitignore.tmpl, not the skill repo .gitignore. If the script must guarantee the
    ignore at runtime for an already-scaffolded workspace, retrofit via the
    kaggle_gateway._append_line_if_absent pattern.
  </action>
  <verify>
    <automated>uv run pytest tests/test_push_kernel.py tests/test_poll_kernel.py -q; python3 -c "g=open('scripts/templates/gitignore.tmpl').read(); assert 'kernel_log.txt' in g and '!experiments/*/kernel_run.json' in g" && grep -q 'run_kaggle' scripts/pull_kernel.py && ! grep -q 'unzip' scripts/pull_kernel.py</automated>
  </verify>
  <acceptance_criteria>
    - `kernels output` files land under experiments/exp-NNN; the pulled log is written to kernel_log.txt and never printed.
    - `pull_kernel.py` routes every call through `run_kaggle`, contains no `unzip`/zip-slip logic, and never `print`s the raw log/output buffer.
    - docker_image + machine_shape are merged into kernel_run.json, preserving existing keys (D-14).
    - gitignore.tmpl ignores `experiments/*/kernel_log.txt` and un-ignores `!experiments/*/kernel_run.json` (tracked provenance).
  </acceptance_criteria>
  <done>pull_kernel.py fetches output+log+image provenance via the gateway into the same contract, stages kernel_log.txt for the recorder, and the gitignore keeps provenance tracked while ignoring artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle API → skill | Status lines, run logs, pulled files, image metadata are untrusted Kaggle text/files |
| kernel log → recorder | The pulled log crosses to record_experiment.py by PATH only; content is pattern-matched, never executed |
| pulled files → workspace | Kernel output lands under experiments/exp-NNN/ — artifacts gitignored, provenance tracked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Info-Disclosure | poll/pull CLI output | mitigate | All calls via run_kaggle (no-echo); status/log buffers never printed; raw output quarantined via dump_last_error |
| T-4-02 | Tampering/EoP | kernel_log.txt content | mitigate | Log written to file and handed to recorder by path; poll/pull derive no executed path/command from log or status content (pure regex/pattern match, V5) |
| T-4-06 | Info-Disclosure | gitignore staging | mitigate | Workspace gitignore ignores kernel_log.txt + pulled artifacts; only small kernel_run.json provenance is tracked; no blanket `git add` in these scripts |
| T-4-05 | Info-Disclosure | off-allowlist egress on output pull | accept | `kernels output` downloads via storage.googleapis.com (already allowlisted, 04-RESEARCH A5); no new host |
| T-4-SC | Tampering | package installs | mitigate | No package install in this plan (stdlib scripts + template edit only) |
</threat_model>

<verification>
- `uv run pytest tests/test_poll_kernel.py -x -q` GREEN (status classify, backoff budget, detach-not-cancel).
- `uv run pytest -q` full suite green (no regression).
- gitignore + pull-source assertions (Task 2 automated verify) pass.
</verification>

<success_criteria>
- Polling reaches a terminal status under a bounded, jittered, 429-safe loop; a transient blip is
  never misread as failure; our-side timeout DETACHES (never cancels) and is resumable.
- Pull lands result.json + artifacts + rendered .ipynb in the same contract the local runner uses,
  writes kernel_log.txt for the recorder, and records image provenance in kernel_run.json.
- The workspace gitignore ignores kernel artifacts + logs while keeping kernel_run.json tracked.
</success_criteria>

<output>
Create `.planning/phases/04-kaggle-kernel-execution-gpu-path/04-03-SUMMARY.md` when done.
Document the kernel_log.txt location + exit-code semantics so 04-04 wires --kernel-log correctly.
</output>
