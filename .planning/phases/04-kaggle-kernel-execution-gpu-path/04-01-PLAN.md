---
phase: 04-kaggle-kernel-execution-gpu-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/kernel_logs/complete_but_threw.txt
  - tests/fixtures/kernel_logs/clean.txt
  - tests/fixtures/kernel_logs/oom.txt
  - tests/fixtures/kernel_logs/nonzero.txt
  - tests/fixtures/status/queued.txt
  - tests/fixtures/status/running.txt
  - tests/fixtures/status/complete.txt
  - tests/fixtures/status/error.txt
  - tests/fixtures/status/cancel_acknowledged.txt
  - tests/fixtures/kernel-metadata.golden.json
  - tests/test_convert_notebook.py
  - tests/test_push_kernel.py
  - tests/test_poll_kernel.py
  - tests/test_record_kernel.py
  - tests/test_kernel_live.py
autonomous: true
requirements: [EXP-05]
user_setup: []

must_haves:
  truths:
    - "Every EXP-05 test-map row has a failing (RED) test that runs without a collection crash before any Phase-4 script exists"
    - "The silent-failure headline property is testable WITHOUT a live GPU: fixture logs (traceback / OOM / clean) drive the recorder assertions"
    - "A golden kernel-metadata.json pins the exact expected generated metadata (internet=false, private=true, competition_sources=[slug])"
    - "One status fixture per KernelWorkerStatus terminal/in-flight token pins the poller's parse contract"
    - "An opt-in live-push integration test stub exists behind the -m live --run-live marker, OUT of the default suite"
  artifacts:
    - path: "tests/fixtures/kernel_logs/complete_but_threw.txt"
      provides: "A COMPLETE-but-threw run log containing a Python traceback marker"
      contains: "Traceback (most recent call last)"
    - path: "tests/fixtures/kernel-metadata.golden.json"
      provides: "Expected generated kernel metadata for a known (username, slug, exp) input"
      contains: "\"is_private\": true"
    - path: "tests/test_record_kernel.py"
      provides: "RED tests for the D-11/D-12 log-scan rung (traceback beats valid result, OOM, clean success)"
      contains: "kernel_error"
    - path: "tests/test_poll_kernel.py"
      provides: "RED tests for status-enum classify, exponential backoff budget, detach-not-cancel"
      contains: "DETACH"
  key_links:
    - from: "tests/test_record_kernel.py"
      to: "tests/fixtures/kernel_logs/complete_but_threw.txt"
      via: "load fixture log + assert FAILED(kernel_error) even with a valid result.json present"
      pattern: "complete_but_threw"
    - from: "tests/test_push_kernel.py"
      to: "tests/fixtures/kernel-metadata.golden.json"
      via: "compare generated metadata against the golden file"
      pattern: "kernel-metadata.golden.json"
---

<objective>
Land the Nyquist Wave 0 RED suite for Phase 4: the fixtures and failing test stubs that pin
the EXP-05 kernel-path contract BEFORE any of the four new scripts (or the recorder extension)
exists. This is the vertical-slice "failing end-to-end test first" step, adapted to a stdlib
CLI skill — the headline correctness property (a kernel that reports COMPLETE but actually threw
is recorded FAILED) becomes testable with synthetic fixture logs and no live GPU.

Purpose: Downstream plans (04-02 convert+push, 04-03 poll+pull, 04-04 recorder extension) turn
these tests GREEN and sample against these fixtures. No script is built without a test already
demanding its behavior.
Output: `tests/fixtures/kernel_logs/*`, `tests/fixtures/status/*`, `tests/fixtures/kernel-metadata.golden.json`,
and RED stubs `tests/test_{convert_notebook,push_kernel,poll_kernel,record_kernel}.py` + a live stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md
@.planning/phases/04-kaggle-kernel-execution-gpu-path/04-VALIDATION.md
@tests/conftest.py
@tests/test_run_local.py

<interfaces>
<!-- Contracts these RED tests pin. Downstream plans implement to satisfy them. -->

Subprocess test harness (tests/conftest.py — REUSE, do not re-implement):
- `run_script(script_name, *args, cwd=None, extra_env=None) -> CompletedProcess` — runs a
  self-locating stdlib script as a subprocess. Import script modules INSIDE test functions
  (never at module top) so collection never crashes on a not-yet-built module.
- `SCRIPTS_DIR = Path(__file__).resolve().parent.parent / "scripts"` (from test_run_local.py).

KernelWorkerStatus enum (04-RESEARCH.md, VERIFIED in bundled kagglesdk):
- Terminal tokens: {COMPLETE, ERROR, CANCEL_ACKNOWLEDGED}
- In-flight tokens: {QUEUED, RUNNING, NEW_SCRIPT, CANCEL_REQUESTED}
- CLI prints: `<slug> has status "KernelWorkerStatus.COMPLETE"` (no --format json on status).

D-11 kernel-error markers the recorder will scan for (04-RESEARCH.md Code Examples):
`Traceback (most recent call last)`, `\nError:`, `\nException:`,
`Your notebook tried to allocate more memory than is available`, `Killed`, `Notebook Exceeded`.

Expected generated kernel-metadata.json schema (04-RESEARCH.md, from `kaggle kernels init`):
id=`<username>/<slug>-exp-NNN`, title=`<slug>-exp-NNN`, code_file=`experiment.ipynb`,
language=`python`, kernel_type=`notebook`, is_private=true, enable_gpu=true, enable_tpu=false,
enable_internet=false, competition_sources=[`<slug>`], dataset_sources/kernel_sources/model_sources=[].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Phase-4 kernel fixtures (logs, status captures, golden metadata)</name>
  <read_first>
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md (Code Examples: log markers, status render, metadata schema; Assumptions A2/A3 — render/format tolerance)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-VALIDATION.md (Wave 0 Requirements checklist)
    - tests/fixtures/pages_all.json (existing fixture-file convention)
  </read_first>
  <action>
    Create the fixture files enumerated in 04-VALIDATION.md Wave 0 Requirements.

    Kernel logs under tests/fixtures/kernel_logs/: `complete_but_threw.txt` MUST contain the
    literal `Traceback (most recent call last)` inside otherwise-normal-looking run output (the
    silent-failure case — a COMPLETE session that threw). `clean.txt` MUST contain NONE of the
    D-11 markers (a normal successful run log). `oom.txt` MUST contain a Kaggle OOM banner
    (`Your notebook tried to allocate more memory than is available`) and/or `Killed`.
    `nonzero.txt` MUST contain `Notebook Exceeded` (resource-limit banner). Keep each a few
    plausible lines; per Assumption A3 the exact live shape is unconfirmed, so make markers the
    load-bearing content, not surrounding formatting.

    Status captures under tests/fixtures/status/: one file per enum token exercised by the
    poller — `queued.txt`, `running.txt`, `complete.txt`, `error.txt`,
    `cancel_acknowledged.txt` — each holding a realistic status line
    `<slug> has status "KernelWorkerStatus.<TOKEN>"`. Add `error.txt` with a trailing
    `Failure message: "<msg>"` line so a test can assert the parser matches the STATUS token and
    never a substring inside the failure body (Pitfall 2).

    Golden metadata tests/fixtures/kernel-metadata.golden.json: the exact expected generated
    metadata for input username=`testuser`, slug=`titanic`, exp=`exp-001`, internet-off,
    enable_gpu default — matching the <interfaces> schema (id `testuser/titanic-exp-001`,
    is_private true, enable_internet false, competition_sources ["titanic"], the four *_sources
    arrays, enable_gpu true, enable_tpu false, kernel_type "notebook", language "python",
    code_file "experiment.ipynb").
  </action>
  <verify>
    <automated>test -f tests/fixtures/kernel_logs/complete_but_threw.txt && grep -q "Traceback (most recent call last)" tests/fixtures/kernel_logs/complete_but_threw.txt && grep -q "Notebook Exceeded" tests/fixtures/kernel_logs/nonzero.txt && grep -q 'KernelWorkerStatus.COMPLETE' tests/fixtures/status/complete.txt && python3 -c "import json; d=json.load(open('tests/fixtures/kernel-metadata.golden.json')); assert d['is_private'] is True and d['enable_internet'] is False and d['competition_sources']==['titanic'] and d['id']=='testuser/titanic-exp-001'"</automated>
  </verify>
  <acceptance_criteria>
    - `complete_but_threw.txt` contains `Traceback (most recent call last)`; `clean.txt` contains none of the six D-11 markers; `oom.txt` contains the OOM banner or `Killed`; `nonzero.txt` contains `Notebook Exceeded`.
    - Each `tests/fixtures/status/*.txt` contains `has status "KernelWorkerStatus.<TOKEN>"`; `error.txt` also carries a `Failure message:` body line.
    - `kernel-metadata.golden.json` parses and asserts is_private=true, enable_internet=false, competition_sources==["titanic"], id=="testuser/titanic-exp-001".
  </acceptance_criteria>
  <done>All Wave 0 fixture files exist and the automated verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Author RED test stubs for every EXP-05 test-map row + live stub</name>
  <read_first>
    - tests/test_run_local.py (subprocess run_script pattern, source-invariant tests, uv shim)
    - tests/conftest.py (run_script fixture, import-inside-function discipline, git/env hygiene)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-VALIDATION.md (Per-Task Verification Map — the exact test node names)
    - .planning/phases/04-kaggle-kernel-execution-gpu-path/04-RESEARCH.md (Test Map + Code Examples for expected behaviors)
  </read_first>
  <action>
    Create four RED test modules whose node names EXACTLY match the 04-VALIDATION.md map so
    downstream plans can run them by name. Import the not-yet-built script modules INSIDE test
    functions (or drive via run_script), never at module top, so pytest collection never aborts
    (conftest discipline).

    tests/test_convert_notebook.py::test_reconvert_idempotent — asserts convert_notebook.py, run
    twice against an unchanged experiment.py (with a `uv` shim like _make_uv_shim), regenerates
    experiment.ipynb non-destructively and leaves experiment.py byte-identical (D-02).

    tests/test_push_kernel.py::test_metadata_golden — asserts push_kernel.py's generated
    kernel-metadata.json (built from a seeded config slug + a stubbed `kaggle config view`
    username) equals tests/fixtures/kernel-metadata.golden.json. ::test_internet_provenance —
    asserts the effective enable_internet value is written into experiments/exp-001/kernel_run.json
    (D-06 guard). Mock the gateway (monkeypatch run_kaggle or a `kaggle` PATH shim) — no live call.

    tests/test_poll_kernel.py::test_status_classify — imports classify_status from poll_kernel and
    asserts each status fixture maps to the correct in-flight/terminal bucket and that a
    `Failure message:` body never yields a false STATUS token. ::test_backoff_budget — with a
    seeded RNG and a mocked/monotonic clock, asserts intervals are exponential, capped, jittered,
    and the loop stops at the wall-clock budget; transient run_kaggle errors are tolerated up to a
    consecutive-failure threshold. ::test_detach_not_cancel — with run_kaggle mocked to stay
    RUNNING past the budget, asserts kernel_run.json.status becomes PENDING/DETACHED and NO
    `kernels cancel`-style call is ever issued.

    tests/test_record_kernel.py::test_traceback_beats_valid_result — writes a VALID result.json
    AND passes --kernel-log pointing at complete_but_threw.txt; asserts meta.json status=FAILED,
    failure_reason=kernel_error. ::test_clean_log_success — clean.txt + valid result ⇒
    status=SUCCESS. ::test_oom_marker — oom.txt ⇒ FAILED(kernel_error). Reuse the
    test_record_experiment.py seeding helpers (config metric, scaffold stub meta.json).

    tests/test_kernel_live.py — one opt-in `@pytest.mark.live` (skipped unless --run-live) stub
    that documents the full push→poll→pull→record loop against a real kernel and the three items
    to confirm live (T4×2 string, log format, status render). It must be SKIPPED in the default
    suite, never collected as a failure.

    Add source-invariant guards (test_run_local.py:107-117 style) that will go GREEN once code
    lands: record_experiment.py source contains `kernel_error`; poll/push/pull route through
    `run_kaggle` and never `print` a raw status/log buffer.
  </action>
  <verify>
    <automated>uv run pytest tests/test_convert_notebook.py tests/test_push_kernel.py tests/test_poll_kernel.py tests/test_record_kernel.py tests/test_kernel_live.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only` succeeds (no collection crash) for all five new test modules despite the scripts not existing yet.
    - Every node named in 04-VALIDATION.md's Per-Task Verification Map is present with the exact name.
    - Running the four mocked modules yields RED (failing/erroring) results now; `test_kernel_live.py` is SKIPPED (not failed) without `--run-live`.
    - No test imports poll_kernel/push_kernel/pull_kernel/convert_notebook at module top level.
  </acceptance_criteria>
  <done>All five test modules exist, collect cleanly, are RED (or skipped for the live stub), and pin the EXP-05 contract by name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle CLI stdout/stderr → skill | Untrusted Kaggle text (status lines, run logs) enters via fixtures here, live later |
| test fixtures → CI | Fixture logs are synthetic, authored in-repo — no live secret risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering/Info-Disclosure | status/log fixtures | mitigate | Fixtures are synthetic and secret-free; they pin the pattern-match-only contract (no path/command derived from log text) that downstream code must honor |
| T-4-02 | Tampering | kernel-error markers | mitigate | Golden markers are literal strings scanned for presence only; tests assert no execution is derived from log content |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No package install in this plan (test data + stdlib pytest only); jupytext gate handled in 04-02 |
</threat_model>

<verification>
- `uv run pytest tests/test_convert_notebook.py tests/test_push_kernel.py tests/test_poll_kernel.py tests/test_record_kernel.py --collect-only -q` succeeds.
- `uv run pytest -q -k "kernel"` shows RED for the four mocked modules and SKIPPED for the live stub (no collection abort).
- Fixture assertions in Task 1's automated verify pass.
</verification>

<success_criteria>
- All Wave 0 fixtures + five test modules exist.
- Every EXP-05 test-map row is represented by a named, RED (or skipped-live) test.
- The headline silent-failure property is expressed as a fixture-driven test needing no live GPU.
</success_criteria>

<output>
Create `.planning/phases/04-kaggle-kernel-execution-gpu-path/04-01-SUMMARY.md` when done.
</output>
