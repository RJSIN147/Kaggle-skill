---
phase: 03-local-experiment-loop-ledger-strategy
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/templates/experiment.py.tmpl
  - scripts/scaffold_experiment.py
  - tests/test_resolve_data_dir.py
  - tests/test_run_cv.py
  - tests/test_scaffold_experiment.py
autonomous: true
requirements: [EXP-01, EXP-02]
must_haves:
  truths:
    - "The AI authors a fresh experiment.py per experiment from a scaffold that ships resolve_data_dir() + run_cv()"
    - "The same experiment.py runs locally or on a Kaggle kernel untouched (backend detection is code, not config)"
    - "run_cv fits the preprocessor on the train fold only and transforms the val fold — leakage-safe by construction"
    - "Custom splitter and custom metric callable are first-class so the AI rarely bypasses the harness"
    - "scaffold_experiment mints a zero-padded exp-NNN, increments state.next_exp_id, and is idempotent"
  artifacts:
    - path: "scripts/templates/experiment.py.tmpl"
      provides: "resolve_data_dir() + run_cv() harness + LightGBM starter + result.json emit"
      contains: "def run_cv"
    - path: "scripts/scaffold_experiment.py"
      provides: "id-cursor mint + template render + meta stub + artifacts/ dir"
      contains: "next_exp_id"
  key_links:
    - from: "scripts/scaffold_experiment.py"
      to: "control/state.json"
      via: "read-increment-write next_exp_id via set_config_field"
      pattern: "set_config_field"
    - from: "scripts/templates/experiment.py.tmpl (run_cv)"
      to: "experiments/exp-NNN/result.json"
      via: "write_text(json.dumps(result))"
      pattern: "result\\.json"
---

<objective>
Ship the experiment authoring surface: `scripts/templates/experiment.py.tmpl` (a plain,
diffable `experiment.py` run locally via `uv run`, NOT a locally-executed `.ipynb` — D-01;
jupytext conversion is deferred to Phase-4 kernel-push) carrying the two load-bearing helpers
`resolve_data_dir()` — the D-03 backend-agnostic Phase-4 seam — and `run_cv()` — the D-07
leakage-safe-by-construction CV harness that emits `result.json` and takes a fixed default
`seed=42` with a per-experiment override recorded in provenance (D-09) — plus
`scripts/scaffold_experiment.py` which mints a fresh `exp-NNN` from `state.next_exp_id` (the
scaffold entry point of the D-02 separate-idempotent-entry-points cycle), renders the template,
writes a meta stub, and creates the `artifacts/` dir.

Purpose: EXP-02 requires the AI to author a fresh script from a template using a backend-agnostic
data path + a result contract so the same code runs local or on a kernel; criterion 1 requires
fold-internal preprocessing enforced. The harness makes leakage-safety structural (fit_transform
on train, transform on val) while keeping custom splitter/metric first-class (the D-07 tension).
Output: the ML-env template, the scaffolder entry point, and Wave-0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-local-experiment-loop-ledger-strategy/03-CONTEXT.md
@.planning/phases/03-local-experiment-loop-ledger-strategy/03-RESEARCH.md
@.planning/phases/03-local-experiment-loop-ledger-strategy/03-PATTERNS.md
@scripts/metric_registry.py

<interfaces>
<!-- resolve_data_dir + run_cv are specified VERBATIM in 03-RESEARCH.md §Pattern 2 (lines 281-293)
     and §Pattern 3 (lines 301-382). Copy them, do not re-derive. -->
From scripts/init_workspace.py (reuse — do not reinvent):
  _render_text(template_name: str, mapping: dict) -> str      # string.Template.safe_substitute
  create_if_absent(path: Path, content: str) -> str
  set_config_field(config_path: Path, key_path: tuple, value) -> int   # works on any control JSON (state.json too)
  WORKSPACE_DIRS loop (init_workspace.py ~322): mkdir(parents=True, exist_ok=True)

run_cv signature (03-RESEARCH.md §Pattern 3, line 328):
  run_cv(*, X, y, model_factory, preprocess_factory=None, feature_fn=None, metric,
         registry_entry, cv_scheme, n_splits=5, seed=42, groups=None, splitter=None,
         exp_dir=".", prediction_type=None)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: experiment.py.tmpl — resolve_data_dir() + run_cv() harness + result.json emit</name>
  <read_first>
    - .planning/phases/03-local-experiment-loop-ledger-strategy/03-RESEARCH.md §Pattern 2 (lines 281-293) and §Pattern 3 (lines 296-384 — the full run_cv source + the anti-leakage note) and §result.json schema (lines 416-450)
    - scripts/analyze_data.py lines 73-136 (`_AV_RUNNER_SRC` — the ONLY existing ML-under-uv-run source: its argparse main(), raise SystemExit(main()), pandas/sklearn imports)
    - scripts/metric_registry.py (from 03-01 — the enum names + sklearn_callable strings the harness resolves; keep metric names consistent)
    - .planning/phases/03-local-experiment-loop-ledger-strategy/03-RESEARCH.md Pitfalls 1,3,4,7 (lines 533-571 — leakage, prediction-type, multiclass averaging, TimeSeriesSplit/GroupKFold)
  </read_first>
  <behavior>
    - resolve_data_dir(slug, override): returns Path(override) if given; else /kaggle/input/<slug> if that dir exists; else Path(__file__).resolve().parents[2] / "data"
    - run_cv fits preprocess_factory() on the TRAIN fold (fit_transform) and only transforms the VAL fold — a spy transformer sees ONLY train-fold rows at fit time (the leakage test)
    - run_cv accepts a custom `splitter=` (bypasses scheme selection) and a callable `metric=` (custom scorer) — both first-class
    - run_cv writes experiments/exp-NNN/result.json with fold_scores[], cv_mean, cv_std, n_folds, metric, seed, prediction_type (D-04 shape)
    - the default seed is 42 and flows into both the splitter and the model, and is echoed into result.json (D-09)
    - proba metrics call predict_proba (positive column for binary); label/raw call predict (Pitfall 3)
    - the template's argparse exposes --exp-dir and --slug (the contract run_local.py shells)
  </behavior>
  <action>Create `scripts/templates/experiment.py.tmpl` — an ML-env script (runs under `uv run --no-sync`, imports pandas/numpy/sklearn) mirroring the shape of `analyze_data._AV_RUNNER_SRC` (module-level helpers + `def main()` + `raise SystemExit(main())`), but delivered as a `.tmpl` rendered by `_render_text`. It is a plain `.py` (D-01), not an executed notebook. Copy `resolve_data_dir(slug, override)` VERBATIM from 03-RESEARCH.md §Pattern 2 and `run_cv(...)` VERBATIM from §Pattern 3 (including `_SPLITTERS`, `_make_splitter`, `_resolve_metric`, and the fold loop that does `pp.fit_transform(Xtr, ytr)` / `pp.transform(Xva)`; keep the `seed=42` default — D-09). Embed a metric name→sklearn_callable resolution that stays consistent with `scripts/metric_registry.py` REGISTRY (the template resolves the callable inside the ML env via `getattr(sklearn.metrics, name)`; keep the enum names identical). Ship a LightGBM starter block the AI edits (model_factory returning a fresh LGBMClassifier/Regressor; unfitted preprocess_factory example). argparse: `--exp-dir` and `--slug`. The harness writes `result.json` to `exp_dir/result.json` (D-05: notebook emits; recorder verifies) and saves OOF to `artifacts/oof.npy`. Use stable-API floors only (pandas>=2.2 behavior, no pandas-3.0-only APIs — Pitfall 6). Add a loud comment on the anti-leakage contract: the AI supplies an UNFITTED preprocess_factory; the harness never accepts a pre-fitted transformer or a two-frame preprocess_fn (§Anti-leakage note).</action>
  <verify>
    <automated>uv run pytest tests/test_resolve_data_dir.py -x -q && uv run pytest tests/test_run_cv.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_resolve_data_dir: override wins; /kaggle/input/<slug> preferred when present (simulate via a temp dir + monkeypatch); else falls back to parents[2]/data
    - test_run_cv (gate with `pytest.importorskip("sklearn")` or a new `ml` marker so the default offline suite stays green): a spy preprocess transformer records the row indices seen at fit() and asserts they are a SUBSET of train indices only (never val) — the leakage-safe assertion
    - test_run_cv: passing a custom `splitter=` and a callable `metric=` both work and produce a valid result dict
    - test_run_cv: result.json written with len(fold_scores)==n_folds and finite cv_mean == mean(fold_scores); seed echoed as 42 by default
    - grep confirms the template resolves metrics via sklearn.metrics inside the ML env (NOT importing scripts/metric_registry at runtime in the kernel path)
  </acceptance_criteria>
  <done>experiment.py.tmpl ships resolve_data_dir + a leakage-safe run_cv that emits a D-04 result.json; both tests green (test_run_cv skips cleanly when sklearn absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scaffold_experiment.py — mint exp-NNN, render template, meta stub</name>
  <read_first>
    - scripts/init_workspace.py: scaffold (lines 301-333), _render_text (95-102), create_if_absent (105-111), set_config_field (475-519), _iso_now (90-92), the fail-clear config read
    - scripts/templates/state.json.tmpl (next_exp_id: 1 — the cursor to read-increment-write)
    - .planning/phases/03-local-experiment-loop-ledger-strategy/03-PATTERNS.md §scaffold_experiment.py (lines 181-190) and 03-RESEARCH.md §Runtime State Inventory (line 523 — the id-cursor semantics)
    - scripts/templates/meta.json.tmpl + VERDICT.md.tmpl (from 03-02 — the stubs to render)
  </read_first>
  <behavior>
    - scaffold_experiment.py --workspace WS --idea "..." --hypothesis "..." reads state.next_exp_id (starts 1), mints f"exp-{n:03d}", writes experiments/exp-NNN/experiment.py from experiment.py.tmpl, creates experiments/exp-NNN/artifacts/, writes a meta.json stub (status pending, idea/hypothesis filled), then set_config_field(state, ("next_exp_id",), n+1)
    - re-running does NOT clobber an existing experiment.py (create_if_absent) and does NOT re-consume the same id after a completed mint (cursor already advanced) — idempotent per D-02
    - a corrupt state.json is left byte-intact and the script returns non-zero (MalformedControlJSON posture)
    - the minted experiment.py contains resolve_data_dir + run_cv (rendered from the template)
  </behavior>
  <action>Create `scripts/scaffold_experiment.py` with the standard self-location + shared-helper import header. argparse: `--workspace`, `--idea`, `--hypothesis` (both prose, passed straight into the meta stub — the AI's idea/hypothesis for EXP-01). Fail-clear read of `control/state.json`; mint `exp_id = f"exp-{n:03d}"` from `next_exp_id`. Create `experiments/exp-NNN/artifacts/` with mkdir(parents=True, exist_ok=True). Render `experiment.py.tmpl` via `_render_text` with slug (from config.json) + exp_id, and `create_if_absent(exp_dir/"experiment.py", ...)`. Render `meta.json.tmpl` into a stub meta.json with status pending/unrun and idea+hypothesis filled (numbers left null — the recorder fills them). Advance the cursor via `set_config_field(state_path, ("next_exp_id",), n + 1)`. Do NOT increment the cursor if the fail-clear read failed. This is the `scaffold` entry point of the D-02 separate-idempotent-entry-points cycle. No sklearn/pandas import (this is stdlib plumbing; the template it writes is the only ML code).</action>
  <verify>
    <automated>uv run pytest tests/test_scaffold_experiment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - After one scaffold: experiments/exp-001/{experiment.py, meta.json, artifacts/} exist; state.next_exp_id == 2
    - The minted experiment.py contains `def run_cv` and `def resolve_data_dir`
    - meta.json stub carries the passed --idea/--hypothesis and null numeric fields
    - Second scaffold mints exp-002 (cursor honored), never overwrites exp-001/experiment.py
    - Corrupt state.json → non-zero exit, state.json bytes unchanged, no experiment folder created
  </acceptance_criteria>
  <done>scaffold_experiment.py mints zero-padded exp-NNN, renders the harness template + meta stub, advances the cursor, idempotent; test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-supplied preprocess/model → run_cv fold loop | Untrusted-ish AI code plugged into the harness must not be able to leak val-fold data into training |
| generated experiment.py → uv run execution | The scaffolded script is code that will be executed locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | CV fold preprocessing (leakage) | mitigate | run_cv owns fit_transform(train)/transform(val); AI supplies an UNFITTED preprocess_factory; harness never accepts a fitted transformer or two-frame preprocess_fn (criterion 1, D-07) |
| T-03-03-02 | Elevation of privilege | generated experiment.py executed via uv run | mitigate | execution is bounded by run_local.py (03-04): `uv run --no-sync` (no network install), timeout, inherits the workspace egress sandbox (no internet by default); no network path in the harness |
| T-03-03-03 | Spoofing | wrong metric prediction-type | mitigate | prediction_type drives predict vs predict_proba (Pitfall 3); multiclass needs a custom metric callable (first-class, Pitfall 4) |
| T-03-03-04 | Repudiation | duplicate/overwritten exp ids | mitigate | zero-padded cursor from state.next_exp_id; create_if_absent never clobbers a prior experiment.py (D-02 idempotency) |
</threat_model>

<verification>
- `uv run pytest tests/test_resolve_data_dir.py tests/test_run_cv.py tests/test_scaffold_experiment.py -x -q` green (test_run_cv skips cleanly if sklearn is absent).
- The leakage spy test proves the preprocessor sees only train-fold rows at fit.
- A scaffolded experiment.py contains both helpers and a runnable `main()`.
</verification>

<success_criteria>
- The AI authors a fresh experiment.py from a scaffold with a backend-agnostic data path + result contract (EXP-02) — same code local or kernel.
- Fold-internal preprocessing is enforced by construction (criterion 1); custom splitter + metric are first-class (D-07 tension resolved).
- scaffold_experiment mints exp-NNN, advances the cursor, and is idempotent (EXP-01, D-02).
</success_criteria>

<output>
Create `.planning/phases/03-local-experiment-loop-ledger-strategy/03-03-SUMMARY.md` when done.
</output>
