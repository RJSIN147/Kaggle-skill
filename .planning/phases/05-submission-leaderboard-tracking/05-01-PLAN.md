---
phase: 05-submission-leaderboard-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/submissions/complete.json
  - tests/fixtures/submissions/pending.json
  - tests/fixtures/submissions/error.json
  - tests/fixtures/submissions/unscored.json
  - tests/fixtures/submissions/empty.json
  - tests/fixtures/submissions/mixed_today.json
  - tests/fixtures/submissions/submit_404.txt
  - tests/fixtures/submissions/submit_upload_failed.txt
  - tests/test_submissions_log.py
  - tests/test_budget.py
  - tests/test_gate_policy.py
  - tests/test_lb_gap.py
  - tests/test_check_submission.py
  - tests/test_submit.py
  - tests/test_fetch_lb.py
  - tests/test_submission_live.py
  - tests/test_run_cv.py
  - tests/test_regen_strategy.py
  - tests/test_no_credential_leak.py
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
user_setup: []

must_haves:
  truths:
    - "Every row of 05-VALIDATION.md's Per-Task Verification Map has a RED test with the EXACT node name the map names"
    - "The submit path is provably testable without ever spending a real submission slot (gateway monkeypatched; fixtures live-captured)"
    - "The two fail-open literals from the real CLI (404 swallow, upload-failed) exist as fixtures so the rc==0-is-not-success guard is testable"
    - "pytest collection never crashes even though no Phase-5 script exists yet (imports live inside test bodies)"
    - "A source-guard test exists that forbids any live-marked test from invoking `competitions submit`"
  artifacts:
    - path: "tests/fixtures/submissions/complete.json"
      provides: "Live-captured SCORED submission row (SubmissionStatus.COMPLETE + string publicScore)"
      contains: "SubmissionStatus.COMPLETE"
    - path: "tests/fixtures/submissions/submit_404.txt"
      provides: "The fail-open 404 stdout literal the CLI prints while exiting 0"
      contains: "Could not find competition"
    - path: "tests/test_submit.py"
      provides: "RED tests: argv shape, fail-open guards, read-back correlation, double-spend refusal, meta.json immutability, live-submit source guard"
      contains: "test_no_live_test_ever_submits"
    - path: "tests/test_budget.py"
      provides: "RED tests: charged-today, ERROR excluded, PENDING included, TZ-parametrized UTC boundary, fail-closed"
      contains: "test_utc_day_boundary"
    - path: "tests/test_gate_policy.py"
      provides: "RED tests: D-06 noise gate matrix, first-submission-clear baseline, D-08 assumed-limit last slot"
      contains: "test_assumed_limit_last_slot"
  key_links:
    - from: "tests/test_submit.py"
      to: "tests/fixtures/submissions/submit_404.txt"
      via: "feed the fail-open stdout to a monkeypatched run_kaggle returning rc=0; assert submit.py reports FAILURE"
      pattern: "submit_404"
    - from: "tests/test_submissions_log.py"
      to: "tests/fixtures/submissions/complete.json"
      via: "parse_status/parse_score against the live-captured shape"
      pattern: "SubmissionStatus"
---

<objective>
Land the Nyquist Wave 0 RED suite for Phase 5: the live-captured fixtures and failing test stubs
that pin the entire submission contract BEFORE any Phase-5 script exists.

This is the phase's defining safety layer. A real `competitions submit` is IRREVERSIBLE and spends
a scarce daily slot, so the only responsible way to build the submit path is to have the tests that
demand its behavior already exist and be provably slot-free (monkeypatched gateway + fixtures
transcribed from RESEARCH's live capture). No Phase-5 script is written without a test already
failing for it.

Purpose: every downstream plan (05-02 harness, 05-03 foundation, 05-04 gate, 05-05 submit/fetch,
05-06 gap/alarm, 05-07 wiring) turns a named subset of these RED tests GREEN and samples against
them.
Output: `tests/fixtures/submissions/*`, seven new RED test modules, RED extensions to three
existing test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submission-leaderboard-tracking/05-CONTEXT.md
@.planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md
@.planning/phases/05-submission-leaderboard-tracking/05-PATTERNS.md
@.planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md
@tests/conftest.py
@tests/test_gateway.py
@tests/test_poll_kernel.py

<interfaces>
Contracts these RED tests pin. Downstream plans implement against them EXACTLY.

Gateway exit codes (05-03 adds these to scripts/kaggle_gateway.py):
  UI_GATE = 77 (exists), LIMIT_NEEDS_USER = 78 (exists)
  VALIDATION_FAILED = 65    (EX_DATAERR)     - D-02 pre-submit validation failed
  SUBMIT_UNSUPPORTED = 69   (EX_UNAVAILABLE) - D-01: competition.type in {code, unknown}
  GATE_BLOCKED = 75         (EX_TEMPFAIL)    - D-05 block-by-default; human may override

Script-local exit codes for submit.py / fetch_lb.py (mirrors poll_kernel.py 0/2/3/4):
  0 = SCORED, 2 = submission FAILED (Kaggle ERROR), 3 = DETACHED (PENDING; re-run fetch_lb),
  4 = transient / fail-closed

scripts/submissions_log.py (05-03) - the ONE submissions.jsonl schema module:
  SUB_STATUSES = ("PENDING", "SCORED", "FAILED")
  SUBMISSION_ROW_KEYS = ("schema_version", "exp_id", "kaggle_ref", "competition_slug", "file",
      "file_sha256", "message", "submitted_at", "status", "public_score", "private_score",
      "scored_at", "override_reason", "error_description")
  parse_status(raw) -> "PENDING"|"SCORED"|"FAILED"|None   # None = unparseable => TRANSIENT
  parse_score(raw)  -> float | None                       # "" -> None, NEVER 0.0
  parse_utc(raw)    -> tz-aware UTC datetime | None       # naive ISO -> UTC (assumption A1)
  charged_today(rows, now_utc) -> int                     # -1 sentinel = cannot count (FAIL CLOSED)
  fetch_submissions(slug, timeout=60) -> list | None      # via run_kaggle; None => fail closed
  read_rows(ws) / write_rows(ws, rows) / append_row(ws, row) / validate_row(row) -> list[str]

scripts/submission_gate.py (05-04) - pure policy fns, no I/O:
  NOISE_K_DEFAULT = 1.0
  is_meaningful(cand_cv, cand_std, best_cv, greater_is_better, k=NOISE_K_DEFAULT) -> bool
      # best_cv is None (no prior submitted experiment) => True (the FIRST submission is CLEAR)
  decide(*, cand_cv, cand_std, best_cv, greater_is_better, remaining, limit_provenance,
         k=NOISE_K_DEFAULT) -> dict
      # {"recommendation": "SUBMIT"|"BLOCKED", "reasons": [...], "warnings": [...],
      #  "requires_confirmation": bool}

scripts/lb_gap.py (05-06) - pure derived-view fns:
  join_cv_lb(sub_rows, ledger_rows) -> list[dict]           # SCORED only; exp_id/cv_mean/lb/gap
  rank_inversions(pairs, greater_is_better) -> list[tuple]  # pairs: [(exp_id, cv_mean, lb_score)]

run_cv harness (05-02) - new optional kwargs, all default None (graceful, D-09):
  run_cv(*, ..., X_test=None, test_ids=None, id_column=None, target_column=None,
         submission_agg=None)
  Writes experiments/exp-NNN/submission.csv FLAT (never under artifacts/) when X_test is given.
  result["submission_path"] = "submission.csv" | None; "submission.csv" appended to
  result["artifacts"] when emitted.

Existing test seams to REUSE (do not re-derive):
  tests/conftest.py :: run_script, tmp_workspace - subprocess harness. Imports of not-yet-built
      modules MUST live INSIDE test bodies so collection never crashes at RED.
  tests/test_gateway.py :: _fake_run_kaggle(rows) -> fake(*argv, timeout=60) -> (0, payload);
      monkeypatch.setattr(<module>, "run_kaggle", fake)  # patched on the IMPORTING module
  tests/test_poll_kernel.py :: _FakeClock (now/sleep, no real waiting), _sequence(items)
  tests/test_run_cv.py :: render_experiment (from test_resolve_data_dir) + pytest.importorskip
      gating for numpy/sklearn - the default offline suite must SKIP, never RED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the live-captured submissions fixtures</name>
  <read_first>
    - .planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md (R1: the two fail-open stdout literals; R2: the EXACT live JSON shape - 7 keys, SubmissionStatus. prefix, string scores)
    - .planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md (Wave 0 Requirements)
    - tests/fixtures/status/complete.txt (the existing one-file-per-shape fixture convention)
    - tests/fixtures/pages_all.json (the existing JSON fixture convention)
  </read_first>
  <action>
    Create tests/fixtures/submissions/ holding shapes TRANSCRIBED from 05-RESEARCH.md R1/R2 live
    capture. Do NOT invent shapes - Phase 2 already learned that hand-built fixtures miss the real
    thing.

    Each .json file is a JSON array of submission objects with EXACTLY the seven live-verified keys
    in this order: ref (int), fileName (str), date (str - NAIVE ISO, no tz suffix, e.g.
    "2025-09-10T11:29:01.560000"), description (str), status (str), publicScore (str),
    privateScore (str).

    complete.json  - one row: status "SubmissionStatus.COMPLETE", publicScore "0.77511",
                     privateScore "", description "exp-007 | cv=0.841230", ref 46780678.
    pending.json   - one row: status "SubmissionStatus.PENDING", publicScore "", privateScore "".
    error.json     - one row: status "SubmissionStatus.ERROR", publicScore "", privateScore "".
    unscored.json  - one row: status "SubmissionStatus.COMPLETE", publicScore "" (the parse-to-None
                     case; must NEVER become 0.0).
    empty.json     - the empty array [].
    mixed_today.json - the budget fixture: at least 5 rows across two FIXED UTC days
                     (2026-07-12 = "today", 2026-07-11 = "yesterday"), mixing COMPLETE, PENDING and
                     ERROR. Include at least one ERROR row dated today (must NOT be counted) and at
                     least one PENDING row dated today (MUST be counted). Include at least one row
                     within 2 hours of a UTC midnight so a TZ-parametrized test can prove the day
                     boundary. Tests inject now_utc, so there is no real-clock dependency.

    submit_404.txt - the CLI's fail-open 404 stdout containing the exact literal
                     "Could not find competition" (the CLI exits 0 while printing this).
    submit_upload_failed.txt - the fail-open upload stdout containing the exact literal
                     "Could not submit to competition", plus a token-shaped sentinel line
                     kagat_TOKENLEAK_SENTINEL_ZZZZ so the no-echo tests have a string to prove is
                     never printed.
  </action>
  <verify>
    <automated>python3 -c "
import json,pathlib
d=pathlib.Path('tests/fixtures/submissions')
KEYS=['ref','fileName','date','description','status','publicScore','privateScore']
for n in ('complete','pending','error','unscored','empty','mixed_today'):
    rows=json.loads((d/(n+'.json')).read_text()); assert isinstance(rows,list), n
    for r in rows: assert list(r)==KEYS, (n, list(r))
c=json.loads((d/'complete.json').read_text())[0]
assert c['status']=='SubmissionStatus.COMPLETE' and c['publicScore']=='0.77511' and isinstance(c['ref'],int)
u=json.loads((d/'unscored.json').read_text())[0]; assert u['publicScore']==''
m=json.loads((d/'mixed_today.json').read_text()); assert len(m)>=5
assert any('ERROR' in r['status'] for r in m) and any('PENDING' in r['status'] for r in m)
assert 'Could not find competition' in (d/'submit_404.txt').read_text()
t=(d/'submit_upload_failed.txt').read_text()
assert 'Could not submit to competition' in t and 'TOKENLEAK_SENTINEL' in t
print('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/submissions/{complete,pending,error,unscored,empty,mixed_today}.json each parse as a JSON array whose objects carry EXACTLY the 7 live-verified keys in order.
    - complete.json carries the fully-qualified literal SubmissionStatus.COMPLETE and a STRING publicScore (not a float).
    - unscored.json carries publicScore "" - the value that must parse to None, never 0.0.
    - mixed_today.json has >=5 rows across two fixed UTC days and includes both an ERROR row (not charged) and a PENDING row (charged) dated today.
    - submit_404.txt contains "Could not find competition"; submit_upload_failed.txt contains "Could not submit to competition" AND a kagat_TOKENLEAK_SENTINEL_ZZZZ sentinel.
    - The automated verify command exits 0.
  </acceptance_criteria>
  <done>All fixture files exist with the live-captured shapes and the verify command passes.</done>
</task>

<task type="auto">
  <name>Task 2: RED tests for the data + decision tier (submissions_log, budget, gate policy, lb_gap)</name>
  <read_first>
    - .planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md (Per-Task Verification Map - the node names below are LOAD-BEARING and must match EXACTLY)
    - .planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md (Pattern 2 parse_status/parse_score; Pattern 3 charged_today + the UTC trap; Pattern 4 rank_inversions; Pattern 5 is_meaningful + k)
    - .planning/phases/05-submission-leaderboard-tracking/05-PATTERNS.md (tests section - the monkeypatch-the-gateway seam; module-level import of the module-under-test is FORBIDDEN)
    - tests/conftest.py
    - tests/test_gateway.py
    - tests/test_experiment_meta.py
    - tests/test_metric_registry.py
  </read_first>
  <action>
    Create four RED test modules. Import the not-yet-built modules INSIDE each test body
    (importlib.import_module("submissions_log")), never at module top - pytest collection must never
    crash at RED. Node names must match 05-VALIDATION.md EXACTLY.

    tests/test_submissions_log.py
      test_parse_status - "SubmissionStatus.COMPLETE" -> "SCORED"; bare "COMPLETE" -> "SCORED";
        "SubmissionStatus.PENDING" -> "PENDING"; "SubmissionStatus.ERROR" -> "FAILED"; garbage,
        None, and an ERROR row whose description merely contains the word COMPLETE -> None (never a
        false terminal). The parse must be ANCHORED, not a substring grep.
      test_parse_score - "" -> None; "0.77511" -> 0.77511; "abc" -> None; None -> None. Assert the
        "" case is NOT 0.0 (guards against a defensive float(x or 0)).
      test_row_schema - validate_row on a well-formed row returns []; a row missing exp_id or
        carrying an unknown status returns non-empty error strings. SUBMISSION_ROW_KEYS is a
        fixed-order tuple of the 14 keys (byte-stable rewrite).
      test_atomic_rewrite - write_rows then read_rows round-trips; the JSONL is one compact
        (separators=(",",":")) row per line, newline-terminated, byte-empty when empty; no .tmp file
        survives.

    tests/test_budget.py - pure-fn tests over tests/fixtures/submissions/mixed_today.json, always
    passing an INJECTED now_utc (never the real clock).
      test_charged_today - counts today's rows, EXCLUDES status ERROR (D-13: Kaggle never charges a
        processing error), INCLUDES status PENDING (the slot was accepted and is being scored).
      test_utc_day_boundary - TZ-parametrized via monkeypatch.setenv("TZ", ...) + time.tzset() over
        "Pacific/Kiritimati" (UTC+14) and "Pacific/Midway" (UTC-11): the count is IDENTICAL. This is
        the test that proves datetime.now() (local) was not used.
      test_fails_closed_when_count_unavailable - THE FAIL-CLOSED CONTRACT (ONE behavior; 05-03 Task 2
        implements it in these same words): charged_today returns the -1 SENTINEL on ANY row with an
        unparseable `status` OR an unparseable `date`. It must NOT silently skip an unrecognized status
        - doing so would undercount the charged submissions (e.g. on a FUTURE Kaggle status literal)
        and let the user submit past Kaggle's real daily limit. A `FAILED` status is the ONLY
        legitimate skip (D-13, not charged). Also: fetch_submissions returns None when the
        monkeypatched gateway returns rc != 0 or a non-array payload. The caller must NEVER guess a
        count.

    tests/test_gate_policy.py - pure matrix test, no I/O (mirror tests/test_metric_registry.py).
      test_noise_gate_matrix - both greater_is_better values; gain > k*cv_std -> meaningful; gain ==
        k*cv_std -> NOT meaningful (strictly greater); gain < k*cv_std -> NOT meaningful; a gain in
        the WRONG direction -> NOT meaningful. k is configurable (assert k=0.0 flips a borderline
        case).
      test_first_submission_is_clear - best_cv is None (no prior submitted experiment) =>
        is_meaningful True and decide() recommends SUBMIT. An empty comparison set must NEVER produce
        a spurious block.
      test_blocked_by_default_when_within_noise - decide() returns recommendation "BLOCKED" and
        requires_confirmation True when the gain is within noise (D-05).
      test_assumed_limit_last_slot - limit_provenance == "assumed_default": a warning naming ASSUMED
        appears on EVERY decision; when remaining == 1 (the LAST assumed slot) requires_confirmation
        is True even when the CV recommendation is SUBMIT (D-08).
      test_zero_remaining_blocks - remaining <= 0 => BLOCKED regardless of the CV signal.

    tests/test_lb_gap.py - pure derived-view tests.
      test_gap_trend - join_cv_lb over synthetic submissions rows x ledger rows: only SCORED rows
        with a non-None public_score appear; PENDING/FAILED/unscored rows are excluded; the gap is
        computed per experiment.
      test_rank_inversion_alarm - fires when CV says B beats A but LB says A beats B; direction-aware
        for BOTH greater_is_better values; does NOT fire when CV and LB agree.
      test_alarm_needs_two_points - with 0 or 1 scored submissions the alarm returns no inversions
        and the renderer emits the honest "needs >=2 scored submissions (have N)" line - it never
        fabricates a signal from one point.
  </action>
  <verify>
    <automated>uv run pytest tests/test_submissions_log.py tests/test_budget.py tests/test_gate_policy.py tests/test_lb_gap.py -q --collect-only 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_submissions_log.py tests/test_budget.py tests/test_gate_policy.py tests/test_lb_gap.py --collect-only -q` exits 0: collection does NOT crash despite submissions_log / submission_gate / lb_gap not existing yet (imports live inside test bodies).
    - The four modules are RED (they fail/error on the missing module) - not passing vacuously, not skipped.
    - These exact nodes are collectable by name: tests/test_budget.py::test_utc_day_boundary, tests/test_gate_policy.py::test_assumed_limit_last_slot, tests/test_lb_gap.py::test_rank_inversion_alarm, tests/test_submissions_log.py::test_parse_status, tests/test_submissions_log.py::test_parse_score.
    - tests/test_budget.py sets TZ via monkeypatch + time.tzset() for BOTH Pacific/Kiritimati and Pacific/Midway and asserts an identical count.
    - No test in these modules spawns a real CLI process; run_kaggle is monkeypatched wherever fetch_submissions is exercised.
  </acceptance_criteria>
  <done>All four RED modules collect cleanly and fail for the right reason (the module under test does not exist yet).</done>
</task>

<task type="auto">
  <name>Task 3: RED tests for the entry points + RED extensions to three existing modules</name>
  <read_first>
    - .planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md (Per-Task Verification Map - exact node names)
    - .planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md (R1 fail-open exit code; Pattern 1 confirm-by-read-back; Pitfall 5 label aggregation; Pitfall 6 write-ordering; Pitfall 7 double-spend; Validation Architecture layers 1-4)
    - .planning/phases/05-submission-leaderboard-tracking/05-PATTERNS.md (tests section - _FakeClock, _sequence, the no-echo sentinel assertion, the source-guard grep, the spy-transformer fold test)
    - tests/test_poll_kernel.py
    - tests/test_no_credential_leak.py
    - tests/test_run_cv.py
    - tests/test_regen_strategy.py
    - tests/test_competition_live.py
  </read_first>
  <action>
    Create four more RED modules and RED-extend three existing ones. Same discipline: imports inside
    test bodies; the gateway is ALWAYS monkeypatched; no real CLI process is ever spawned.

    tests/test_check_submission.py
      test_refuses_non_csv_type - config competition.type in {"code", "unknown", null} => exit 69
        (SUBMIT_UNSUPPORTED) and the monkeypatched run_kaggle is NEVER called (the captured argv list
        is empty). The refusal message names why (a code competition submits a kernel version, not a
        CSV).
      test_never_submits - across ALL check_submission code paths (clear, blocked, validation-failed)
        the captured argv never contains "submit". check_submission is FREE.
      test_validation_matrix - parametrized over the four D-02 failures, each producing exit 65 and a
        PRECISE message naming the mismatch: header_mismatch, row_count_mismatch, id_set_mismatch
        (order-independent set compare), blank_prediction (blank / nan / NA / null / inf). Plus the
        happy path -> exit 0.
      test_sample_resolution_ladder - resolves via control/raw/competition-type-signals.json ->
        signals.submission_csv_in_manifest (assert it picks gender_submission.csv, NOT a guessed
        sample_submission.csv), then a data/*submission*.csv glob, then a data/test.csv first-column
        fallback, then exit 65 when nothing exists. Assert the chosen file is PRINTED so a human can
        spot a wrong pick.

    tests/test_submit.py - the slot-safety core.
      test_argv_shape - the captured argv is exactly ("competitions","submit",<slug>,"-f",<csv path>,
        "-m",<message starting with the exp_id>) and NEVER contains "-k", "-v" or "--sandbox"
        (--sandbox is a host-admin flag, not a dry run).
      test_fail_open_404_is_not_success - run_kaggle returns (0, <submit_404.txt>) => submit.py
        reports FAILURE, exits non-zero, and writes NO SCORED row. rc==0 is NOT proof.
      test_fail_open_upload_is_not_success - run_kaggle returns (0, <submit_upload_failed.txt>) =>
        same; also assert the TOKENLEAK sentinel from that fixture never appears in stdout/stderr.
      test_unconfirmed_submission_fails_closed - rc==0 with no failure literal, but the read-back
        returns no row whose description starts with our exp_id => submit.py must NOT claim success
        and must NOT record a SCORED row.
      test_correlates_by_exp_id - the read-back matches on the description prefix (anchored
        ^exp-\d{3}\b) AND date >= the submit start time; the recovered Kaggle ref (int) is written
        into the submissions.jsonl row.
      test_pending_row_written_before_poll - the PENDING row (exp_id, ref, file_sha256) is persisted
        BEFORE the poll loop begins, so a crash mid-poll never loses the provenance of a spent slot
        (Pitfall 6). Assert by making the poll status_fn raise, then reading the file.
      test_refuses_double_spend - an existing non-FAILED row with the same exp_id AND the same
        file_sha256 => refuse without spending and point to fetch_lb.py; --resubmit overrides.
      test_meta_json_untouched - experiments/exp-NNN/meta.json is byte-identical before and after a
        full successful submit (D-11: the LB score is NEVER written back).
      test_dry_run_never_calls_gateway - --dry-run prints the exact argv it WOULD pass and exits 0
        with the captured argv list empty.
      test_no_live_test_ever_submits - SOURCE GUARD: for every tests/test_*live*.py, assert the source
        contains neither the string "competitions submit" nor the tuple form "competitions", "submit".
        Makes the irreversibility rule enforced, not remembered.

    tests/test_fetch_lb.py - reuse _FakeClock + _sequence from test_poll_kernel.py (no real sleeping).
      test_detach_preserves_pending - the status stays PENDING past the wall-clock budget => the loop
        DETACHES (exit 3), the submissions.jsonl row remains PENDING with its ref, and no cancel-style
        argv is ever issued. Assert the injected clock advanced to >= the budget and every sleep is
        <= the LB cap (full jitter).
      test_idempotent_resume - a PENDING row + a read-back showing COMPLETE => the row transitions in
        place to SCORED with a parsed float public_score and a scored_at; a SECOND run is a no-op
        (same bytes) and never re-submits.
      test_error_row_becomes_failed - a read-back ERROR => the row becomes FAILED with
        error_description null (the CLI does not expose the reason) and the message points the user at
        the Kaggle submissions page rather than fabricating a cause.
      test_reconcile_backfills_out_of_band - --reconcile back-fills rows present on Kaggle but absent
        locally; a row whose description has no exp-NNN prefix gets exp_id null.

    tests/test_submission_live.py - a @pytest.mark.live CLI-drift canary (excluded by the default
      -m 'not live'). READ-ONLY: competitions submissions <slug> --format json --page-size 200 only.
      Asserts the seven keys and the SubmissionStatus. prefix still hold. It must contain NO submit
      call (the source guard above enforces this).

    RED-extend three existing modules (leave their existing tests untouched and green):
      tests/test_run_cv.py - keep the pytest.importorskip numpy/sklearn gating (the default offline
        suite must SKIP, never RED).
        test_submission_optional - run_cv WITHOUT X_test still writes a valid result.json and NO
          submission.csv; WITH X_test/test_ids/id_column/target_column it writes
          <exp_dir>/submission.csv FLAT (not under artifacts/), sets result["submission_path"] and
          appends "submission.csv" to result["artifacts"].
        test_label_aggregation_is_not_mean - for a label metric (accuracy - TITANIC'S DEFAULT) the
          emitted prediction column contains ONLY members of the training label set (e.g. {0,1});
          assert no value like 0.4/0.6 appears. This is the trap that would otherwise spend a real
          slot on garbage.
        test_test_preds_use_fold_preprocessor - extend the existing spy-transformer pattern: each
          fold's test prediction goes through THAT fold's fitted pp (the transform call sees the test
          rows), so the anti-leakage contract holds for test too.
      tests/test_regen_strategy.py
        test_lb_block_rendered - regen_strategy renders a CV->LB facts block from submissions.jsonl x
          ledger.jsonl; the AI --reasoning-file is still spliced VERBATIM; the full atomic overwrite is
          preserved; with <2 scored rows the honest "needs >=2" line appears instead of an alarm.
      tests/test_no_credential_leak.py
        Extend the existing scan to the new scripts (check_submission.py, submit.py, fetch_lb.py,
        submissions_log.py): a token-shaped sentinel in a fake CLI buffer must never reach
        stdout/stderr; it is quarantined via dump_last_error.
  </action>
  <verify>
    <automated>uv run pytest tests/test_check_submission.py tests/test_submit.py tests/test_fetch_lb.py tests/test_submission_live.py tests/test_run_cv.py tests/test_regen_strategy.py tests/test_no_credential_leak.py -q --collect-only 2>&1 | tail -3 && uv run pytest tests/test_submit.py::test_no_live_test_ever_submits -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All seven modules collect without a crash (--collect-only exits 0) even though check_submission / submit / fetch_lb do not exist yet.
    - `uv run pytest tests/test_submit.py::test_no_live_test_ever_submits -x -q` is GREEN immediately (it is a source guard, not a behavior test) and tests/test_submission_live.py contains no "competitions submit".
    - tests/test_run_cv.py and tests/test_regen_strategy.py keep every pre-existing test passing: `uv run pytest tests/test_regen_strategy.py -q` shows the previously-green nodes still green and only the new test_lb_block_rendered red.
    - tests/test_run_cv.py retains its pytest.importorskip gating - the new D-09 tests SKIP (never RED) when numpy/sklearn are absent.
    - Every SCORE-01 node named in 05-VALIDATION.md exists verbatim, including test_fail_open_404_is_not_success, test_fail_open_upload_is_not_success, test_unconfirmed_submission_fails_closed, test_refuses_double_spend, test_meta_json_untouched, test_detach_preserves_pending, test_idempotent_resume.
  </acceptance_criteria>
  <done>All RED entry-point modules collect cleanly; the source-guard test is green; the three extended modules keep their existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture -> test assertion | Fixture text emulates Kaggle-authored (untrusted) CLI output |
| test suite -> Kaggle API | Any live-marked test crosses a real, billable network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Denial of Service (scarce irreversible resource) | any test invoking `competitions submit` | mitigate | tests/test_submit.py::test_no_live_test_ever_submits greps every tests/test_*live*.py for `competitions submit` and fails if found. Every unit test monkeypatches run_kaggle; no CLI process is spawned. |
| T-05-01-02 | Information Disclosure | fixture CLI buffers echoed by a script under test | mitigate | submit_upload_failed.txt carries a kagat_TOKENLEAK_SENTINEL_ZZZZ sentinel; tests/test_no_credential_leak.py asserts it never reaches stdout/stderr for any new script. |
| T-05-01-03 | Tampering | hand-invented fixture shapes drifting from the real CLI | mitigate | Fixtures are TRANSCRIBED from 05-RESEARCH.md's live capture (7 keys, SubmissionStatus. prefix, string scores); tests/test_submission_live.py is a read-only drift canary. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No package is installed by this phase (05-RESEARCH.md: "This phase adds zero new dependencies"). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest --collect-only -q` succeeds for the whole suite (no collection crash from the new modules).
- `uv run pytest tests/test_submit.py::test_no_live_test_ever_submits -x -q` is GREEN.
- The pre-existing suite is unbroken except for the deliberately-RED new nodes.
- `grep -rn "competitions submit" tests/ | grep -v test_submit.py` returns nothing.
</verification>

<success_criteria>
- All 8 fixture files and 7 new RED test modules exist; 3 existing modules RED-extended.
- Every row of 05-VALIDATION.md's Per-Task Verification Map maps to an existing test node name.
- Collection never crashes; the source guard is green; no test can spend a submission slot.
</success_criteria>

<output>
Create `.planning/phases/05-submission-leaderboard-tracking/05-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
