---
phase: 05-submission-leaderboard-tracking
plan: 07
type: execute
wave: 4
depends_on: [05-02, 05-04, 05-05, 05-06]
files_modified:
  - SKILL.md
  - references/kaggle-cli-behavior.md
  - scripts/templates/gitignore.tmpl
autonomous: false
requirements: [SCORE-01, SCORE-02, SCORE-03]
user_setup: []

must_haves:
  truths:
    - "SKILL.md sequences check_submission -> [the human decides] -> submit -> fetch_lb, and the human confirmation lives in the SKILL, never in an input() inside a script"
    - "SKILL.md's gate protocol branches on the EXACT new exit codes 65 / 69 / 75, alongside the existing 77 / 78"
    - "references/kaggle-cli-behavior.md records the observed competitions submit + competitions submissions shapes, including the fail-open exit-code trap"
    - "The .gitignore posture for experiments/*/submission.csv is a CONSCIOUS, stated decision, not an accident"
    - "The A1 assumption (is submissions.date UTC?) is verified against a real clock at the first real submission and the result is written into the fixture doc"
  artifacts:
    - path: "SKILL.md"
      provides: "Three new script rows + the submit gate protocol (65/69/75) + the check -> decide -> submit -> fetch sequence"
      contains: "check_submission.py"
    - path: "references/kaggle-cli-behavior.md"
      provides: "The submit + submissions fixture entries (fail-open literals, the 7-key shape, the SubmissionStatus. prefix, no quota command, the A1 UTC finding)"
      contains: "Could not find competition"
    - path: "scripts/templates/gitignore.tmpl"
      provides: "The stated decision that submission.csv stays ignored (provenance lives in the file_sha256)"
      contains: "submission.csv"
  key_links:
    - from: "SKILL.md"
      to: "scripts/check_submission.py"
      via: "the gate protocol: exit 75 => the SKILL asks the human, then re-invokes submit.py --confirm"
      pattern: "75"
    - from: "references/kaggle-cli-behavior.md"
      to: "scripts/submit.py"
      via: "the recorded fail-open literals are the ones submit.py matches"
      pattern: "Could not submit to competition"
---

<objective>
Wire the three new entry points into the skill's human loop, record the live CLI behavior this phase
discovered, and close the one assumption that genuinely cannot be closed without spending a slot.

The scripts never block on stdin - SKILL.md holds the human submit/don't-submit loop via the
reserved-exit-code gate protocol (Phase 2 D-10). D-05's block-by-default with an informed human
override is only real once SKILL.md knows to stop on exit 75, present the decision material, and
re-invoke `submit.py --confirm` only after the human says so.

`references/kaggle-cli-behavior.md` currently has NO submit/leaderboard entries at all. This phase
discovered a load-bearing fact - `kaggle competitions submit` is FAIL-OPEN, exiting 0 on a 404 slug and
on a failed upload - and that belongs in the checked-in fixture doc, not just in a planning artifact.

Finally, assumption A1 (is `submissions.date` UTC?) is UNPROVABLE without a real submission at a known
wall-clock time, and the budget's day boundary depends on it. It is gated here behind a human-verify
checkpoint at the first real end-to-end submission.
Output: SKILL.md, the CLI-behavior fixture doc, an explicit .gitignore decision, and a verified A1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-submission-leaderboard-tracking/05-CONTEXT.md
@.planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md
@.planning/phases/05-submission-leaderboard-tracking/05-PATTERNS.md
@.planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md
@SKILL.md
@references/kaggle-cli-behavior.md
@scripts/templates/gitignore.tmpl

<interfaces>
The three entry points to document (D-14), in sequence:
  check_submission.py --workspace <ws> --exp-id exp-NNN
      FREE - never spends a slot. Exits: 0 = clear | 65 = validation failed | 69 = wrong competition
      type | 75 = gate-blocked (not recommended) | 77 = UI gate.
  submit.py --workspace <ws> --exp-id exp-NNN --confirm [--reason "..."] [--resubmit] [--dry-run]
      SPENDS THE SLOT. Exits: 0 = SCORED | 2 = submission FAILED | 3 = DETACHED (PENDING) | 4 =
      transient/fail-closed | 69 | 77 | 124 | 127.
  fetch_lb.py --workspace <ws> [--exp-id exp-NNN] [--reconcile]
      The detach fallback. Never submits. Same 0/2/3/4 codes.

Reserved exit codes SKILL.md must branch on (the EXACT values):
  65 VALIDATION_FAILED | 69 SUBMIT_UNSUPPORTED | 75 GATE_BLOCKED | 77 UI_GATE | 78 LIMIT_NEEDS_USER

D-07: the override reason is OPTIONAL. SKILL.md must NEVER force the user to type a justification to
spend their own slot. Record it when given; never demand it.

.gitignore facts (VERIFIED): scripts/templates/gitignore.tmpl ALREADY contains `experiments/*/*.csv`,
so submission.csv is ignored today. control/submissions.jsonl is ALREADY tracked (no rule covers it).
kaggle_gateway._append_line_if_absent is the established RETROFIT mechanism if a rule ever needs to
reach an already-scaffolded workspace - editing the template alone does NOT update an existing one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SKILL.md - the submit gate protocol and the three script rows</name>
  <read_first>
    - SKILL.md (the WHOLE file - the "Gate protocol - the SKILL is the only waiter (D-10)" section around lines 167-180, and the "Scripts (progressive disclosure)" table around lines 361-382; copy the row density and the "what it guarantees" voice)
    - .planning/phases/05-submission-leaderboard-tracking/05-CONTEXT.md (D-05 block-by-default + informed override; D-07 the reason is OPTIONAL; D-08 the assumed-limit last slot; D-14 the three entry points)
    - scripts/check_submission.py, scripts/submit.py, scripts/fetch_lb.py (the actual flags and exit codes as built)
    - scripts/kaggle_gateway.py (the reserved-code table)
  </read_first>
  <action>
    Extend SKILL.md. Keep it LEAN - every line is a recurring per-session token cost (the body persists
    in context once invoked), so add rows and a protocol block, not prose.

    1. SCRIPTS TABLE - add three rows in the established voice (name what the script GUARANTEES, not
       what it "does"):
       - scripts/check_submission.py | SCORE-01/03 D-02/04/05 the FREE pre-submit gate: refuses a
         non-CSV competition (69), validates submission.csv against the sample (exact headers, exact
         row count, order-independent id set, no blanks/NaN -> 65), counts the KAGGLE-authoritative
         remaining budget (UTC day, ERROR rows not charged), and renders the decision material +
         recommendation. NEVER spends a slot (exit 0 = clear / 75 = blocked)
       - scripts/submit.py | SCORE-01 D-01/03 spends the slot ONLY when explicitly confirmed. The CLI
         is FAIL-OPEN (exits 0 on a 404 slug and on a failed upload), so success is CONFIRMED BY
         READ-BACK, never by the exit code; the PENDING row is written BEFORE the poll so a spent slot
         is never lost; bounded poll then DETACH (exit 0/2/3/4). --dry-run prints the exact argv without
         calling Kaggle
       - scripts/fetch_lb.py | SCORE-01/02 D-03/11 the detach fallback: re-runnable, transitions a
         PENDING submissions row to SCORED/FAILED in place; --reconcile back-fills out-of-band
         submissions from Kaggle. NEVER submits

    2. GATE PROTOCOL - extend the existing "the SKILL is the only waiter" section with the three new
       codes, in the same voice as the existing 77/78 entries. Reiterate the standing rule at the top of
       that section: the scripts NEVER poll, sleep, or block on stdin.
       - Exit 69 (SUBMIT_UNSUPPORTED) from check_submission.py / submit.py: competition.type is `code`
         or `unknown`. The CSV path REFUSES rather than risk a slot. Tell the user plainly that a code
         competition submits a pushed KERNEL VERSION, not a CSV upload, and that this path is out of v1.
         Do NOT retry, do NOT work around it, and do NOT attempt a submit.
       - Exit 65 (VALIDATION_FAILED) from check_submission.py: the submission file does not match the
         sample. Surface the EXACT mismatch the script printed (header / row count / id set / blank
         values), fix the experiment or the harness, re-run the experiment, and re-invoke
         check_submission.py. NEVER submit an unvalidated file.
       - Exit 75 (GATE_BLOCKED) from check_submission.py: ⭐ THE HUMAN DECISION POINT (D-05).
         The framework has TAKEN A POSITION: this submission is NOT RECOMMENDED (the CV gain does not
         beat fold-noise, or the budget is exhausted, or this is the LAST ASSUMED slot). Present the
         decision material the script printed VERBATIM - this experiment's CV +/- std, the best
         already-submitted CV, the margin vs the noise bound (k), the remaining slots today, the
         CV-to-LB divergence state, and any ASSUMED-budget warning. Then ASK the user whether to submit
         anyway. Only if they explicitly confirm, re-invoke
         `submit.py --exp-id exp-NNN --confirm [--reason "..."]`.
         ⚠ --reason is OPTIONAL (D-07). NEVER require the user to justify spending their own slot; pass
         --reason only when they volunteered one.
         ⚠ NEVER auto-confirm. NEVER submit on the user's behalf without an explicit go-ahead. This is
         PROJECT.md's core principle: human-in-the-loop reasoning is the point; opaque budget-burning
         agents are the anti-pattern.

    3. SEQUENCE - document the submit flow where the experiment loop is described:
       run/record -> `check_submission.py` (FREE) -> [the SKILL presents the material; THE HUMAN
       DECIDES] -> `submit.py --confirm` -> (on exit 3) `fetch_lb.py` -> `regen_strategy.py` (which now
       renders the CV-to-LB trend + the divergence alarm).
       Note that submit.py exit 3 means DETACHED, not failed: the slot IS spent and the PENDING row IS
       recorded; re-run fetch_lb.py later to record the score.

    ⚠ Do NOT document, hint at, or build final selection / nomination (D-12 - the user explicitly scoped
    it out of v1, a deliberate deviation from roadmap plan 05-02). The submissions history and the
    divergence alarm are what the user nominates from, manually, in the Kaggle UI.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q -k "skill or manifest" 2>/dev/null; python3 -c "
import re,pathlib
s=pathlib.Path('SKILL.md').read_text()
for tok in ('check_submission.py','submit.py','fetch_lb.py','75','65','69','--confirm'):
    assert tok in s, tok
assert re.search(r'GATE_BLOCKED|75 \(', s), 'gate protocol missing exit 75'
low=s.lower()
assert 'nominat' not in low and 'final selection' not in low, 'D-12 must NOT be documented'
print('SKILL.md ok, lines:', len(s.splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md's scripts table gains exactly three rows: `scripts/check_submission.py`, `scripts/submit.py`, `scripts/fetch_lb.py`, each naming what the script GUARANTEES and its exit codes.
    - SKILL.md's gate-protocol section gains entries for exit 65, 69 and 75 alongside the existing 77 and 78, and the exit-75 entry explicitly instructs Claude to present the decision material and ASK the user, and to re-invoke `submit.py --confirm` only on an explicit go-ahead.
    - The exit-75 entry states that `--reason` is OPTIONAL and that the user must never be required to justify spending their own slot (D-07).
    - SKILL.md documents the sequence check_submission -> [human decides] -> submit -> fetch_lb -> regen_strategy, and states that submit.py exit 3 = DETACHED (the slot IS spent; re-run fetch_lb).
    - `grep -in "nominat\|final selection" SKILL.md` returns NOTHING (D-12 is not documented or hinted at).
    - `grep -n "input(" SKILL.md` shows no instruction for a script to block on stdin; the human loop is held by the SKILL.
    - SKILL.md stays under 500 lines (the progressive-disclosure budget - the body is a recurring per-session cost).
  </acceptance_criteria>
  <done>SKILL.md sequences the three entry points and holds the human submit/don't-submit loop via exit codes 65/69/75.</done>
</task>

<task type="auto">
  <name>Task 2: references/kaggle-cli-behavior.md + the explicit .gitignore decision</name>
  <read_first>
    - references/kaggle-cli-behavior.md (the WHOLE file - the existing Phase 2/4 entry format, the provenance-line convention, the confidence tagging)
    - .planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md (R1 the submit surface + the fail-open table; R2 the submissions shape, the 7-key allow-list, the status literals, the NO-quota-command finding, the pagination dead-end; the "Fixture entries for references/kaggle-cli-behavior.md" section is written to be pasted in)
    - scripts/templates/gitignore.tmpl (experiments/*/*.csv ALREADY sweeps up submission.csv; control/submissions.jsonl is ALREADY tracked)
    - scripts/kaggle_gateway.py (_append_line_if_absent - the retrofit mechanism, if one were ever needed)
    - tests/test_gitignore.py (substring-based, not golden - a comment addition is safe)
  </read_first>
  <action>
    A. references/kaggle-cli-behavior.md - add the submit + submissions entries. This doc currently has
    NO submit/leaderboard entries; CONTEXT flags this as a real deliverable, not a formality. Follow the
    existing entry format and provenance-line convention, and record:

    `kaggle competitions submit` (CLI 2.2.3)
      - Invocation: `competition` is POSITIONAL (not -c). `-m/--message` is REQUIRED and round-trips
        into `description` on read-back - it is the ONLY exp_id correlation channel, because the CLI
        DISCARDS the submission `ref` the API returns.
      - ⚠ FAIL-OPEN EXIT CODE (the load-bearing finding): a bad/closed slug (404) exits **0** while
        printing `Could not find competition`; a failed upload exits **0** while printing `Could not
        submit to competition`. Auth (401), a 403 gate, and a half-given code-comp flag pair exit 1.
        The SUCCESS message is SERVER-AUTHORED - do NOT parse it. ⇒ rc==0 is NOT proof; confirm by
        READ-BACK.
      - ⚠ `--sandbox` is a competition-HOST/ADMIN flag, NOT a dry run. Never reach for it as a test mode.
      - Submit does NOT block until scored: the row appears PENDING and is scored asynchronously.

    `kaggle competitions submissions <slug> --format json --page-size 200` (CLI 2.2.3)
      - EXACTLY seven fields exist (confirmed by triggering the CLI's own projection error): date,
        description, fileName, privateScore, publicScore, ref, status.
      - `status` is FULLY QUALIFIED: `SubmissionStatus.PENDING` / `.COMPLETE` / `.ERROR` - never bare.
      - `publicScore` / `privateScore` are STRINGS, and `""` when unscored/withheld - parse with a
        guarded float(); never fabricate 0.0.
      - `date` is a NAIVE ISO string with NO timezone suffix (see the A1 entry from Task 3).
      - Sort is newest-first; ERROR rows ARE returned (GROUP_ALL), which is what makes the
        "errors are not charged" rule mechanizable.
      - `--page-size` default 20, max 200. ⚠ `--page-token` is UNUSABLE - the CLI discards
        next_page_token, so there is no token to chain. One page of 200 always covers today.
      - `errorDescription` exists in the API model but is NOT exposed by the CLI - a FAILED submission's
        reason is not retrievable. Do not fabricate one.
      - ⚠ There is NO submission-quota command. `kaggle quota` is GPU/TPU HOURS ONLY (live-verified).
        The daily submission budget must be derived by COUNTING ROWS.

    Provenance line (honest, verbatim): "Captured 2026-07-12 against CLI 2.2.3 in the project .venv by
    (a) --help, (b) reading the installed package source, and (c) READ-ONLY `competitions submissions` /
    `quota` calls against titanic. **`competitions submit` was never executed - no submission slot was
    spent.** No credential value was read, printed, or recorded."
    Leave a clearly-marked placeholder for the SUCCESS-PATH entry and the A1 UTC finding, which Task 3's
    checkpoint fills in from the first real submission.

    B. scripts/templates/gitignore.tmpl - make the submission.csv posture an EXPLICIT, STATED DECISION
    rather than an accident. `experiments/*/*.csv` already sweeps up submission.csv.
    DECISION (adopted, per RESEARCH): LEAVE IT IGNORED. Do NOT add a `!experiments/*/submission.csv`
    negation. Rationale to write into the template as a comment beside the existing rule: a submission
    CSV is a heavy artifact, consistent with data/, *.npy and artifacts/; provenance is preserved BETTER
    by the `file_sha256` recorded in control/submissions.jsonl (which D-11 requires anyway, and which
    proves exactly which bytes were uploaded); and reproducibility is preserved by the TRACKED
    experiment.py + seed + git_commit.
    Also add a comment confirming that control/submissions.jsonl is TRACKED by design (small, diffable,
    git-backed provenance - the same posture as control/raw/*.json per Phase 2 D-03) and that no rule
    covers it.
    ⚠ Change ONLY comments - do NOT add or remove any ignore rule. If a rule ever WERE added, note in
    the comment that kaggle_gateway._append_line_if_absent is the retrofit mechanism, because editing
    this template does NOT update an already-scaffolded workspace.
  </action>
  <verify>
    <automated>uv run pytest tests/test_gitignore.py tests/test_init_workspace.py -q && python3 -c "
import pathlib
r=pathlib.Path('references/kaggle-cli-behavior.md').read_text()
for tok in ('competitions submit','Could not find competition','Could not submit to competition',
            'SubmissionStatus.','--sandbox','page-token','quota','competitions submissions'):
    assert tok in r, tok
assert 'never executed' in r.lower() or 'no submission slot was spent' in r.lower()
g=pathlib.Path('scripts/templates/gitignore.tmpl').read_text()
assert 'submission.csv' in g and 'submissions.jsonl' in g
assert '!experiments/*/submission.csv' not in g, 'submission.csv must STAY ignored'
assert 'experiments/*/*.csv' in g, 'the existing ignore rule must be unchanged'
print('docs + gitignore ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `references/kaggle-cli-behavior.md` gains a `competitions submit` entry recording: the POSITIONAL competition arg, the REQUIRED `-m` message as the only exp_id correlation channel, the FAIL-OPEN exit-code table with BOTH literals verbatim, that the success message is server-authored and must not be parsed, and that `--sandbox` is a host-admin flag and not a dry run.
    - It gains a `competitions submissions` entry recording: the exactly-seven-field allow-list, the fully-qualified `SubmissionStatus.` literals, that scores are STRINGS and `""` when unscored, that `date` is naive with no tz suffix, that ERROR rows are returned, that `--page-token` is unusable, and that `kaggle quota` is GPU/TPU-only so the budget must be counted from rows.
    - The provenance line states plainly that `competitions submit` was NEVER executed and no slot was spent, and that no credential value was read or printed.
    - A clearly-marked placeholder exists for the success-path entry and the A1 UTC finding (Task 3 fills it).
    - `scripts/templates/gitignore.tmpl` still contains `experiments/*/*.csv` and does NOT contain `!experiments/*/submission.csv` - submission.csv stays ignored BY DECISION, and the rationale (heavy artifact; provenance lives in the file_sha256 in submissions.jsonl; reproducibility lives in the tracked experiment.py + seed + git_commit) is written in a comment.
    - The template comments confirm `control/submissions.jsonl` is tracked by design and note `_append_line_if_absent` as the retrofit mechanism.
    - `git diff scripts/templates/gitignore.tmpl` shows COMMENT-ONLY changes - no ignore rule added or removed.
    - `uv run pytest tests/test_gitignore.py tests/test_init_workspace.py -q` GREEN.
  </acceptance_criteria>
  <done>The live CLI behavior is checked in, and the submission.csv gitignore posture is a stated decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: First real end-to-end submission - verify the A1 UTC assumption</name>
  <read_first>
    - .planning/phases/05-submission-leaderboard-tracking/05-RESEARCH.md (Assumptions Log A1; Open Question 1 - this is the ONE thing that genuinely requires a live slot, and it comes free with the first real submission)
    - .planning/phases/05-submission-leaderboard-tracking/05-VALIDATION.md (Manual-Only Verifications - both rows)
    - references/kaggle-cli-behavior.md (the placeholder left by Task 2)
  </read_first>
  <what-built>
    The complete submit path is built and fully green under mock: check_submission.py (free validation +
    the Kaggle-authoritative budget count + the D-05 decision material), submit.py (fail-open guard,
    confirm-by-read-back, PENDING-row-before-poll, bounded poll + detach), fetch_lb.py (resume +
    reconcile), the CV-to-LB trend and rank-inversion alarm in strategy.md, and the harness that emits
    submission.csv.

    Two things CANNOT be proven without a real submission, and both are gated here:
    1. **A1 (MEDIUM confidence, HIGH impact):** `submissions.date` is a NAIVE ISO string with no
       timezone suffix. The budget model treats it as UTC. If it is not UTC, the daily count is silently
       wrong near the day boundary - the framework could refuse a submission the user is entitled to, or
       permit one over the limit.
    2. The `competitions submit` SUCCESS-path output (server-authored, deliberately never parsed by the
       code - recorded for the fixture doc only).
  </what-built>
  <how-to-verify>
    Run ONE real, human-supervised submission. This spends exactly one real, irreversible slot - that is
    the point, and it is unavoidable by definition.

    1. In a workspace with a scored experiment, run:
       `uv run python scripts/check_submission.py --workspace <ws> --exp-id exp-NNN`
       Confirm it prints the full decision material (CV +/- std, best submitted CV, the margin vs the
       noise bound with k stated, remaining slots today, any ASSUMED-budget warning) and exits 0 or 75.
       Confirm it did NOT submit anything.
    2. Note the CURRENT UTC WALL-CLOCK TIME (`date -u`). Write it down.
    3. Run: `uv run python scripts/submit.py --workspace <ws> --exp-id exp-NNN --confirm`
       (Add `--reason "..."` ONLY if you want to; it is optional.)
    4. Confirm the script does NOT claim success from the exit code alone - it should report that the
       submission was CONFIRMED BY READ-BACK, and print the recovered Kaggle `ref`.
    5. Inspect `control/submissions.jsonl`: the row must carry exp_id, kaggle_ref, file_sha256, and
       status PENDING or SCORED.
    6. ⭐ **THE A1 CHECK:** run
       `uv run python -c "import sys;sys.path.insert(0,'scripts');from submissions_log import fetch_submissions;print(fetch_submissions('<slug>')[0]['date'])"`
       and compare the returned `date` to the UTC wall-clock you noted in step 2.
       - If they MATCH (within the submission latency): `date` IS UTC. **A1 CONFIRMED.**
       - If they differ by your LOCAL UTC offset: `date` is LOCAL, and the budget's day boundary is
         WRONG - report this, because the budget model must then be corrected.
    7. Confirm `experiments/exp-NNN/meta.json` is UNCHANGED (D-11 immutability).
    8. Record the outcome in `references/kaggle-cli-behavior.md`, filling the placeholder from Task 2:
       the A1 verdict (UTC or not, with the observed values), and the observed success-path submit output
       (redacting nothing sensitive - it carries no credential, but do not paste raw buffers wholesale).
  </how-to-verify>
  <acceptance_criteria>
    - One real submission was made, human-supervised, and `control/submissions.jsonl` carries exactly one new row for it with exp_id, kaggle_ref and file_sha256 populated.
    - `submit.py` reported success only AFTER the read-back confirmation (it did not claim success from rc==0).
    - `experiments/exp-NNN/meta.json` is byte-identical before and after (D-11).
    - The A1 verdict is recorded in `references/kaggle-cli-behavior.md` with the observed submit-time UTC clock and the returned `date` value, and is explicitly marked CONFIRMED or REFUTED.
    - If A1 is REFUTED, the budget's day-boundary handling is flagged as a BLOCKER for correction before the phase is marked complete.
    - `uv run pytest -q` is fully green afterwards, and `uv run pytest -m live tests/test_submission_live.py -x` passes (the read-only drift canary).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the A1 verdict (CONFIRMED / REFUTED), or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md instructions -> Claude's behavior | The gate protocol is the ONLY thing standing between an agent and an auto-submitted slot |
| human -> the submit decision | The decision must reach a human, not be inferred by the model |
| Kaggle API -> the first real submission | The one place in this phase where a real, irreversible slot is spent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | Claude auto-confirming a blocked submission | mitigate | The exit-75 gate-protocol entry explicitly instructs Claude to PRESENT the material and ASK, and to pass --confirm ONLY on an explicit human go-ahead. The scripts never block on stdin and never self-confirm; --confirm must be typed into the invocation by the SKILL only after the human says yes. This is PROJECT.md's anti-opaque-budget-burning-agent principle. |
| T-05-07-02 | Repudiation | the user being coerced into justifying a slot spend | accept (by design) | D-07: --reason is OPTIONAL. SKILL.md is explicitly instructed never to require it. Recorded when volunteered; never demanded. |
| T-05-07-03 | Tampering (silent budget miscount) | assumption A1 - `date` may not be UTC | mitigate | Gated behind the blocking human-verify checkpoint: the first real submit compares the noted UTC wall-clock to the returned `date`. A REFUTED A1 is a BLOCKER, not a footnote. The verdict is written into references/kaggle-cli-behavior.md. |
| T-05-07-04 | Information Disclosure | committing a heavy/derived artifact or pasting a raw CLI buffer into the fixture doc | mitigate | submission.csv stays gitignored BY DECISION (provenance lives in the file_sha256 in submissions.jsonl). The fixture doc records SHAPES and LITERALS, never a raw buffer, and the provenance line records that no credential value was read or printed. |
| T-05-07-05 | Spoofing | documenting a code-competition or nomination path that does not exist / is out of scope | mitigate | D-01 and D-12 are both explicitly documented as REFUSED / NOT BUILT. SKILL.md's exit-69 entry tells the user plainly that a code competition needs a kernel version, and instructs Claude not to work around it. `grep -in "nominat\|final selection" SKILL.md` must return nothing. |
| T-05-07-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing (zero new dependencies). |
</threat_model>

<verification>
- `uv run pytest -q` fully green (the whole mock suite, live excluded).
- `uv run pytest -m live tests/test_submission_live.py -x` green (read-only drift canary).
- `uv run ruff check scripts/` clean.
- `grep -in "nominat\|final selection" SKILL.md scripts/ references/` returns nothing (D-12 is nowhere).
- `references/kaggle-cli-behavior.md` contains both fail-open literals, the 7-field allow-list, the no-quota-command finding, and a resolved A1 verdict.
</verification>

<success_criteria>
- SKILL.md holds the human submit/don't-submit loop via exit codes 65/69/75; no script blocks on stdin.
- The live CLI submit/submissions behavior is checked into references/kaggle-cli-behavior.md.
- The submission.csv gitignore posture is a stated decision, not an accident.
- Assumption A1 is CONFIRMED (or refuted and flagged) against a real clock at the first real submission.
</success_criteria>

<output>
Create `.planning/phases/05-submission-leaderboard-tracking/05-07-SUMMARY.md` when done.
</output>
</content>
